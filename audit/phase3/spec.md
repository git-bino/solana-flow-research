# Solana Bonding-Curve Flow Continuation Study

## Судалгааны бүрэн спецификаци ба гүйцэтгэлийн prompt-ууд

**Хувилбар:** v1.3
**Судалгааны удирдагч:** Claude (chat)
**Гүйцэтгэл:** Claude Code
**Аудит:** ChatGPT

**v1.0 → v1.1 өөрчлөлт:** §3 (f7b), §4.1 (үндсэн нүд), §5 (exit fill), §6 (тестийн бүртгэл), §7 Phase 0 (fee handling, дата хэмжээ), Phase 3 (power analysis, эдийн засгийн босго, stratified hazard), Phase 5 (embargo), §12 (ажиллах үеийн систем — шинэ).

**v1.1 → v1.2 өөрчлөлт:** §1.1 (эмпирик y₀, шимтгэл 2.5%), §1.2 (mayhem-ийн нөлөө), §2.2 (mayhem stratum, USDC), §2.3 (NET sol_amount, шинэ баганууд, x_post шууд унших), §3 ба §4 (цонх slot-оор), §7 Phase 0 (шалгалт 1 → 1a/1b/1c, §0.3 хаагдсан, §0.4 хасагдсан).

**v1.2 → v1.3 өөрчлөлт (архитектурын хувилбар):**

- §2.1 — эх сурвалж: Dune үндсэн, BigQuery **хасагдсан** (бүтцийн шалтгаанаар)
- §2.2 — насны таслалт **байхгүй** (N = ∞), түүврийн хувь **100%**
- §2.3 — schema нь **burst мөр**, түүхий event биш
- §2.4 — архитектур **burst-only**: feature/label нь Dune SQL дотор тооцогдоно
- §3 — feature-ийн цонхны хил **мөрийн түвшинд**
- §4.2 — label-ийн хил = **trigger мөр**, slot биш
- §4.3 — `incl_pre` конвенц, slot-индекстэй тогтмол 75-урттай траектор, **үнэлэх цэгийн дүрэм**
- §6.1 — holdout нь **татах үедээ** салгагдана, өөр billing мөчлөгт
- §8.2 — 8-р шаардлага **хоёр хэлбэртэй**: синтетик unit test + SQL↔Python parity

> **Ажлын горим:** энэ spec нь семантикийг тодорхойлно. Хэрэгжүүлэлтийн тоо (баганы яг жагсаалт, хэмжсэн зардал, хугацаа) нь `docs/` доорх тайлангуудад, шийдвэрийн түүх нь `decisions.md`-д, дата руу харсан тест бүр `test_log.md`-д байна. Зөрчил гарвал spec давамгайлна; spec тодорхойгүй бол хэрэгжүүлэлт таамаглахгүй, асууна.

---

## 0. Судалгааны асуулт

**Үндсэн асуулт:**

> Migration хийгээгүй pump.fun bonding curve дээр, t хүртэлх ажигласан урсгалыг мэдэх нь дараагийн τ хугацааны цэвэр урсгалын нөхцөлт тархалтыг өөрчилдөг үү?

**H₀ (null):** Нөхцөлт тархалт = нөхцөлгүй тархалт. Ажигласан урсгал ирээдүйн урсгалын талаар мэдээлэл агуулаагүй.

**H₁:** Тархалт өөрчлөгддөг, бөгөөд тэр өөрчлөлт нь зардлыг давахуйц хэмжээтэй.

**Чухал:** Чиглэлийг урьдчилж заахгүй. "Их ачаа = аюултай" болон "их ачаа = хүчтэй момент" хоёулаа боломжит хариулт. Дата шийднэ.

**Хамрах хүрээ:** Зөвхөн pre-migration (bonding curve) үе шат. Post-migration AMM бол тусдаа судалгаа.

---

## 1. Математик суурь (тогтмол, маргаангүй)

### 1.1 Bonding curve

pump.fun-ий curve нь virtual reserve-тэй constant product:

```
x · y = k
x₀ = 30 SOL          (virtual SOL reserve)
k  = x₀ · y₀

```

> x₀, y₀, k нь тогтмол биш. Токен бүрийн createevent-ээс уншина. Эмпирик (81,052 токен): y₀ = 1,073,000,000 (base unit 1.073e15), x₀ = 30 SOL. Spec v1.1-ийн 1,073,000,191 нь 0 токен дээр таарсан.

`x` = 30 + (хуримтлагдсан бодит SOL). Migration threshold ≈ 85 бодит SOL → x ≈ 115.

**Spot үнэ:** `P(x) = x² / k`

**q SOL-ын худалдан авалт:**

```
Δy = y − k/(x+q)
дундаж төлсөн үнэ = x(x+q) / k

```

**Өөрийн slippage (яг):**

```
slip(q, x) = q / x

```

**Хоцролтын зардал** (чамаас өмнө V SOL land хийвэл):

```
lat(V, x) = (1 + V/x)² − 1

```

**Round-trip breakeven:**

```
BE = (1+V/x)² · (1+q/x)² · (1+fees) − 1
fees ≈ 0.025 (pump.fun 95bp + creator 30bp ≈ 1.25% × 2 тал)

```

> pump.fun 95bp + creator 30bp ≈ 1.25%/тал. Шимтгэл нь curve-ээс ГАДУУР: sol_amount нь NET, fee/creator_fee тусдаа багана. Reserve математикт хутгалдахгүй, зардлын загварт тусдаа давхарга.

### 1.2 Overhead supply (ачаа)

Curve дээр үнэ детерминистик тул **эзэмшигч бүрийн орох үнэ яг мэдэгдэнэ**. Энэ нь ямар ч order-book зах зээлд байхгүй давуу тал.

Wallet w-ийн cost basis:

```
cb(w) = Σ(SOL_in) / Σ(tokens_received)   [FIFO биш, weighted average]

```

Sell гарвал tokens_received-ээс хасаж, cost basis-ийг хэвээр үлдээнэ.

**Ачааны хэмжигдэхүүн (үндсэн feature):**

```
OH(t) = Σ_{w: cb(w) < P(t)} [ tokens_held(w) × (P(t) − cb(w)) ]

```

Энэ нь SOL-оор илэрхийлэгдсэн, одоогийн үнээр бодогдсон **биелээгүй ашгийн нийт хэмжээ**.

**Заавал хэвийлэх:**

```
OH_ratio(t) = OH(t) / x(t)

```

Дан тоо токенууд хооронд харьцуулагдахгүй.

> Cost basis нь бодит SOL/токеноос бодогддог тул virtual reserve-ийн reparameterization (mayhem) түүнд нөлөөлөхгүй. P(t), x(t) шууд уншигдана. Иймд OH, OH_ratio, slippage mayhem токен дээр ч зөв ажиллана.

**Төвлөрөл (хоёрдогч):**

```
OH_conc(t) = дээд 3 wallet-ийн OH-д эзлэх хувь
OH = 0 бол OH_conc = 0   (v1.3: төвлөрөл тодорхойлогдохгүй тул 0, NULL биш)

```

Учир нь ижил OH_ratio-той хоёр токен өөр эрсдэлтэй: биелээгүй ашиг нэг whale дээр төвлөрсөн бол ганц транзакцаар бүхэлдээ буух боломжтой; тархсан бол зарах шийдвэрүүд корреляцгүй.

---

## 2. Дата

### 2.1 Эх сурвалж (v1.3-д дахин эрэмблэгдсэн)

1. **Dune Analytics** — Solana decoded tables (`pumpdotfun_solana`). **Үндсэн эх сурвалж.**
2. **BigQuery Solana public dataset** — **ХАСАГДСАН.** Шалтгаан нь зардал биш, бүтэц: `Instructions` табл дээр `parent_index` 100% NULL, `Transactions`-д instruction-ийн nested бүтэц байхгүй, `log_messages` хоосон → inner (CPI) instruction энэ dataset-д огт байхгүй. pump.fun-ий TradeEvent нь self-CPI-аар ирдэг тул `virtual_sol_reserves`, `virtual_token_reserves`, `fee`, `creator_fee`, `mayhem_mode` зарчмын хувьд хүрэхгүй. Мөн CPI-гаар дуудагдсан арилжаа (aggregator, bundle, bot) бүхэлдээ алга — идэвхтэй корреляцтай хамрах хүрээний нүх, §2.2-ийн хоригийн доор. **BigQuery account нь Dune-ийн хамрах хүрээг бие даан тоолох аудит хэрэгсэл болж үлдэнэ.**
3. Yellowstone gRPC өөрөө бичих. **Phase 0-д битгий эхэл.**

> `balance_changes` + `pre/post_token_balances` зам (арилжааны дүнг decode-гүйгээр гаргах) нь **хорио дор**: `virtual_sol_reserves` уншигдахгүй тул §7-ийн шалгалт 1a/1b доройтоно, `mayhem_mode` уншигдахгүй тул §2.2-ийн stratum хил тодорхойлогдохгүй. Авч үзэх шийдвэр гарвал тусдаа урьдчилсан бүртгэл шаардана.

### 2.2 Universe (survivorship-ээс хамгаалах)

**ЧУХАЛ:** Universe нь *launch* дээр тодорхойлогдоно, *migration* дээр биш.

```
Universe = [T_start, T_end] хооронд үүсгэгдсэн БҮХ pump.fun токен

```

Migrate хийсэн эсэх нь **label**, шүүлт биш. Migrate хийсэн токенууд дээр л судалгаа хийвэл survivorship bias шууд орно.

Хугацаа: `[2026-05-10, 2026-08-08)` — яг 90 хоног. Event-үүдийг +7 хоног (2026-08-15 хүртэл) татна. Сүүлийн 30% нь **frozen holdout** (§6.1).

**Түүврийн хувь = 100% (v1.3).** §2.4-ийн burst-only архитектур нь бүрэн universe-ийг budget-д багтаадаг тул түүвэрлэлт хасагдсан. v1.1-ийн "mint hash-аар тогтмол хувь" гэсэн заалт нь хүчингүй болсон боловч **дүрэм нь хэвээр**: хэрэв ирээдүйд багасгах шаардлага гарвал зөвхөн токеноор санамсаргүй түүвэрлэнэ. Идэвх, эзлэхүүн, наслалт, migration-аар шүүхийг **хатуу хориглоно**.

**Насны таслалт: БАЙХГҮЙ (N = ∞, v1.3).** v1.2-ийн үед budget-ийн шахалтаас болж N = 5 мин авч үзэгдсэн бөгөөд урьдчилж бүртгэсэн дүрмээр сонгогдсон. burst-only архитектур батлагдсаны дараа тэр шахалт алга болсон тул урьдчилж бүртгэсэн нөхцөлт буцаалт идэвхжиж, таслалт хасагдсан. Estimand = "pump.fun токений бүх burst".

**mayhem:** launch дээр тогтоогддог (median onset 0s, 0/81,052 дараа авсан; mayhem нь токен дотор тогтмол — 74,733/74,733 токен дээр MIN = MAX) тул real-time мэдэгдэнэ, survivorship оруулахгүй. Universe-ээс **ХАСАХГҮЙ**. Үндсэн нүд = mayhem бус. mayhem нь §3e-ийн rug хуваалттай ижил, урьдчилж зарлагдсан тусдаа давхарга болж зэрэгцүүлэн тайлагдана. Сигнал зөвхөн mayhem дээр гарвал энэ нь хүчинтэй боловч **ХОЁРДОГЧ** үр дүн — үр дүн харсны дараа үндсэн болгож дэвшүүлэхгүй.

**Quote нэгж:** SOL-quote curve дээр л судална. USDC-quote нь арилжааны 6.9% — хэмжих нэгжийн шүүлт, идэвхийн биш.

### 2.3 Extract-ийн schema (v1.3: burst мөр)

v1.2 хүртэл schema нь **event мөр** байсан. v1.3-д локал руу гарах нэгж нь **burst мөр** болов (§2.4). Түүхий event нь Dune дотор л уншигдана.

**Баганы яг жагсаалт** (49 багана, тип, тодорхойлолт) нь `docs/phase0_extract_schema.md`-д. Spec нь бүлгүүд ба семантикийг тодорхойлно:


| Бүлэг             | Агуулга                                                                                                                        | Заалт        |
| ----------------- | ------------------------------------------------------------------------------------------------------------------------------ | ------------ |
| **Identity**      | `token_mint`, `burst_slot`, `burst_time`, `trigger_tx_index`, `trigger_ix_index`, `trigger_wallet`, `launch_time`, `token_age` | §4.1         |
| **Stratum**       | `mayhem` (токен түвшинд), `quote_mint`                                                                                         | §2.2         |
| **Curve state**   | `depth_x` (trigger мөрийн `x_post`), `y`, `k`, `curve_progress`, `P_t`                                                         | §1.1, f4, f5 |
| **Feature**       | f1 (net_flow 5/12/25 slot), f2 (accel), f3 (n_buyers_12slot), f6 (burst_age), f8 (size_cv_25slot), f9 (round_frac_25slot)      | §3           |
| **OH бүлэг**      | `oh`, `oh_ratio` (f7), `oh_conc` (f7b), `oh_n_wallets`                                                                         | §1.2         |
| **Forward label** | `fwd_net_flow_5/12/37`, `x_at_plus5/12/37`                                                                                     | §4.2         |
| **Hazard**        | `nf3_traj_75` (үндсэн, `incl_pre`), `nf3_traj_75_excl_pre` (robustness), `traj_len`, `death_age`, `censored`                   | §4.3         |
| **Latency**       | L1/L2/L3-ийн үеийн ажигласан урсгал V                                                                                          | §5           |


**Burst мөрөнд БАГТАХГҮЙ** (тусад нь татагдана, ил бүртгэгдсэн):

- f10, f11 — сүлжээ даяарх минутын aggregate (тусдаа таталт)
- f12 `cluster_seen` — Phase 6
- rug label — §3e, ex-post, тусдаа query
- Phase 4-ийн feature бүлгүүд, Phase 6-ийн санхүүжилтийн graph
- Placebo когортын burst бус агшнууд — §6.6

**Түүхий event-ийн семантик** (Dune дотор, локал руу гарахгүй):

```
sol_amount     NET SOL, curve-д ороход. Эмпирикээр батлагдсан:
               эхний арилжаа 74,733/74,733 токен дээр x₀=30-ыг
               яг гаргасан (gross 1.47%)
x_post         event-ийн virtual_sol_reserves-ээс ШУУД уншигдана
x_pre          = x_post − (±sol_amount), мөр дотор локал

```

> Cumulative replay хийхгүй — event дутсан ч дараагийн мөрүүд бохирдохгүй, mayhem-ийн үсрэлт алдаа биш тэмдэглэгдсэн үйл явдал болно. Мөнгөн дүнг бүхэл base unit-ээр (lamport, token unit) боловсруулна.
>
> Цувааны дотоод уялдааг §7 Phase 0-ийн шалгалт 1a/1b/1c шалгана.

### 2.4 Архитектур (v1.3: burst-only)

**Хэмжсэн бүтэц:** Dune дээр тооцоолол хямд, экспорт үнэтэй.

```
credits ≈ [0.64 + 0.31 × сая_event_scan] + [2.36 × үр_дүн_MB]

```

Иймд **feature ба label нь Dune SQL дотор тооцогдож, burst тутамд нэг мөр гарна.** 90 хоногийн бүрэн universe ≈ 1.17M мөр = бүх event гаргахаас ~200 дахин хямд, Free plan-ийн 1–1.5 сарын budget.

**Гүйцэтгэлийн дүрэм:**

- Extract нь **10 хоногийн хэсгээр** ажиллана (timeout-ийн нөөц; timeout болбол execution credit шатна тул асимметр)
- Эхлээд **нэг** хэсгийг ажиллуулж бодит credit хэмжинэ, дараа нь бодит тоогоор экстраполяци хийж үлдсэнийг шийднэ
- Экспорт нь **API pagination**-аар (CSV нь 32,000 мөрөөр таслагддаг)
- Credit-ийн бүх урьдчилсан босгыг **3 дахин маржтай** тавина

**Локал хадгалалт:** ClickHouse. `ORDER BY (token_mint, burst_slot, trigger_tx_index, trigger_ix_index)`. Түүхий event биш, burst мөр хадгална.

**Дараалал:** нэг транзакц дотор pump.fun-ий хэд хэдэн арилжаа байж болно (bundle, aggregator) тул `(slot, tx_index)` хүрэлцэхгүй — `ix_index` заавал орно.

**Sanity:** нийт burst тоо, өдрийн burst тоо, `traj_len = 75` бүх мөр дээр, `tokens_any_event ≤` нийт токен.

---

## 3. Feature-ууд (бүгд backward-looking)

**Хатуу дүрэм:** t мөчид тооцогдох feature нь зөвхөн `block_time ≤ t` датаг ашиглана. Ямар ч ирээдүйн мэдээлэл орж болохгүй. Кодын хяналтын гол зүйл (§8.2-ийн 8-р шаардлага).

**Цонхны хил — МӨРИЙН түвшинд (v1.3):**

```
slot ∈ (s − w, s],  бөгөөд slot = s дотор
(tx_index, ix_index) ≤ одоогийн мөр

```

Slot дотор **өмнө** гүйцэтгэгдсэн арилжаа нь бодит өнгөрсөн тул хасахгүй. Хасах ёстой нь зөвхөн **хожим** гүйцэтгэгдсэн нь.

> Хэрэгжүүлэлтийн анхааруулга: `RANGE ... PRECEDING` нь `ORDER BY slot` дээр тухайн slot-ыг хуваалцсан БҮХ мөрийг peer гэж frame-д оруулна — үүнд хожим гүйцэтгэгдсэн арилжаа ч. Энэ нь §3-ийн дүрмийн шууд зөрчил бөгөөд SQL↔Python parity-гаар баригдахгүй (хоёр тал ижил алдааг давтана). f3/f8/f9 дээр яг ийм согог илэрч засагдсан. Зөв хэрэгжүүлэлт: prefix-difference (ROWS бүтэн түлхүүрийн эрэмбээр) эсвэл нягт slot торон дээр урьдчилан aggregate хийгээд equi-join.


| ID  | Нэр                              | Тодорхойлолт                                                                                                  |
| --- | -------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| f1  | net_flow_5slot / 12slot / 25slot | Trailing цонх дахь (buy SOL − sell SOL)                                                                       |
| f2  | accel                            | net_flow_5slot / (net_flow_25slot / 5)                                                                        |
| f3  | n_buyers_12slot                  | Trailing 12 slot дахь ялгаатай худалдан авагч хаягийн тоо                                                     |
| f4  | depth                            | Одоогийн x                                                                                                    |
| f5  | curve_progress                   | (x − 30) / 85                                                                                                 |
| f6  | burst_age                        | Burst эхэлснээс хойших slot (§4.1)                                                                            |
| f7  | **OH_ratio**                     | §1.2 — гол шинэ feature                                                                                       |
| f7b | OH_conc                          | §1.2 — **зөвхөн robustness.** Phase 3-ийн амьд/үхэх шийдвэрт оролцохгүй                                       |
| f8  | size_cv                          | Trailing 25 slot дахь арилжааны хэмжээний CV                                                                  |
| f9  | round_frac                       | Trailing 25 slot дахь дугуй тоотой (0.1/0.5/1.0 SOL) арилжааны эзлэх хувь                                     |
| f10 | mkt_active_bursts                | Тэр минутад burst-тэй байгаа токений тоо (сүлжээ даяар)                                                       |
| f11 | mkt_total_flow                   | Тэр минутын pump.fun-ий нийт SOL урсгал                                                                       |
| f12 | cluster_seen                     | **Phase 6 хүртэл битгий тооц.** Bool: оролцож буй cluster-ийн аль нэг нь өмнө өссөн токен дээр гарч байсан уу |


> f10, f11 нь сүлжээ даяарх минут тутмын aggregate-аас уншигдана (тусдаа таталт). Slot руу хөрвүүлэхгүй — эх сурвалж нь минутын мөр учир. Энэ нь санаатай, зөрчил биш.

---

## 4. Label-ууд

### 4.1 Burst тодорхойлолт

```
burst_start(t): net_flow_5slot(t) ≥ max(3 SOL, 0.10 × x(t))
                AND түүнээс өмнөх 25 slot-д burst идэвхтэй байгаагүй

```

**ҮНДСЭН НҮД (урьдчилж зарлагдсан):**

```
босго = 0.10 × x,  τ = 12 slot

```

Амьд/үхэх шийдвэр **зөвхөн энэ нүд дээр** гарна. Бусад босго (0.05x, 0.20x) ба τ (5 slot, 37 slot) нь robustness шалгалт: тэдгээрийн чиглэл үндсэн нүдтэй зөрвөл сэрэмжлүүлэг болно, гэхдээ үндсэн нүдийг орлохгүй. Энэ дүрэм нь 9 нүдийн аль нэгэнд санамсаргүй significant гарахад судалгаа "амьд үлдэх" боломжийг хаана.

> block_time нь 1s мөшгилттэй тул 2s цонх ±50% алдаатай. Slot ≈ 400ms бөгөөд §5-ийн latency scenario аль хэдийн slot-д бэхлэгдсэн (L1 = 1 slot). Бүх цонх, τ slot-оор тодорхойлогдоно.

> **Нэгжийн зөрүү, ил бүртгэгдсэн:** §4.1-ийн trigger нь 5-slot цонх, §4.3-ийн alive нь 3-slot. 5-slot дээр илэрсэн burst нь 3-slot цонхноос хурдан "гарна". Урьдчилж бүртгэгдсэн тодорхойлолт тул хэвээр, гэхдээ §3c-ийн hazard муруйн эхлэлийг тайлбарлах хүчин зүйл.

### 4.2 Forward label-ууд

**Label-ийн хил = TRIGGER МӨР, slot биш (v1.3).** Trigger мөрөөс хойших бүх зүйл ирээдүй — үүнд `slot = s` дотор `(tx_index, ix_index) >` trigger байх арилжаа ч.

Симметр: §3-д feature талд `(tx_index, ix_index) ≤ мөр` нь өнгөрсөн гэж тогтоогдсон тул label талд `>` нь ирээдүй байх ёстой. Хоёр өөр хил зэрэг ажиллавал `fwd_price_ret(τ)` нь `fwd_net_flow(τ)`-ийн тоолоогүй урсгалаас хөдөлж, Phase 3-ийн харьцуулалт тайлбарлагдахгүй болно.

τ ∈ {5, 12, 37 slot} тус бүрд:

```
fwd_net_flow(τ)  = trigger мөрөөс хойших, slot ≤ s+τ бүхий
                   бүх арилжааны (buy SOL − sell SOL)
x_at_plus(τ)     = slot ≤ s+τ бүхий СҮҮЛЧИЙН мөрийн x_post
                   (эрэмбэ: (slot, tx_index, ix_index))
fwd_price_ret(τ) = P(t+τ)/P(t) − 1                    [raw, гүйцэтгэлгүй]
                   P(t) = trigger мөрийн x_post-оос
fwd_net_ret(τ)   = §5-ийн зардлын загвараар бодсон    [гол эдийн засгийн label]

```

> "Ижил блокийн арилжааг барьж чадахгүй" гэдэг нь §5-ийн fill model-ийн асуудал, label-ийнх биш. Label бол юу болсны үнэн; §5 нь юуг барьж авах боломжтойг шийднэ. L1 = 1 slot тул ижил slot-ын урсгал эдийн засгийн үнэлгээнд аль хэдийн хасагдана.

### 4.3 Hazard label

```
burst_alive(a): burst эхэлснээс a slot-ын дараа net_flow_3slot > 0 хэвээр үү
death_age:      хамгийн бага a ∈ 1..75 бөгөөд nf3(a) ≤ 0
censored:       ийм a байхгүй бол

```

**Траекторын бүтэц (v1.3):**

- Индекс нь **slot**, event биш. `a = 1..75`, burst_slot-оос хойших slot зөрүү
- Урт нь **бүх мөр дээр яг 75** (`traj_len` assert)
- `nf3(a)` нь нягт slot тор дээрх **rolling 3-slot нийлбэр**: `slot ∈ (a−3, a]`
- Хоосон slot нь нийлбэрт 0 нэмнэ, **гэхдээ цонх өмнөх slot-уудыг агуулсаар**. "Slot-д event байхгүй → nf3 = 0" гэдэг нь **буруу** — нэг slot-ын завсрыг үхэл гэж тэмдэглэнэ
- Нэг slot-д хэд хэдэн event байвал тухайн slot-ын урсгал = **нийлбэр** (`sum(signed_lam)`)

**Цонхны конвенц:** `incl_pre` **(үндсэн).** `nf3(a)` нь `a = 1, 2` дээр burst_slot-оос **өмнөх** slot-уудыг агуулна — өөрөөр хэлбэл nf3 нь бүх `a` дээр ижил trailing 3-slot цонх. `nf3_traj_75_excl_pre` нь robustness багана болж extract-д үлдэнэ.

> Үндэслэл нь муруйн хэлбэр биш, **тодорхойлолтын нэгдмэл байдал**: цонхыг a = 1, 2 дээр тайрвал nf3 нь тэр хоёр цэг дээр a = 10 дээрхээс өөр зүйлийг хэмжиж, цаг хугацаанаас хамаарсан тодорхойлолт үүснэ.

**Үнэлэх цэгийн дүрэм (v1.3, хоёр талд адил):**

```
Үнэлэх цэгээс (slot, tx_index, ix_index) ≤ байгаа бүхэн өнгөрсөн.

```

- Feature талд үнэлэх цэг = **trigger мөр** → slot `s` хэсэгчлэн өнгөрсөн
- Траектор талд үнэлэх цэг = `s+a` **мөч** → slot `s` бүхэлдээ өнгөрсөн

Иймд траекторын `a` дахь элемент нь slot `s`-ийн бүх арилжааг (trigger-ээс өмнөх ч, хойшхи ч) агуулах нь **зөв** — тэдгээр нь `s+a` мөчид аль хэдийн өнгөрсөн. Энэ нь §4.2-ийн мөрийн хилтэй зөрчилдөхгүй; хоёулаа ижил дүрмийн өөр үнэлэх цэг дэх хэрэглээ.

---

## 5. Зардлын загвар (fill model)

**Энэ бол судалгааны амин чухал хэсэг.** Fill assumption буруу бол бүх үр дүн утгагүй.

```python
def realistic_entry_price(x_at_signal, q, latency_ms, observed_flow_during_latency):
    # 1. latency хугацаанд land хийсэн бусдын урсгал
    V = observed_flow_during_latency
    x_eff = x_at_signal + V
    # 2. өөрийн impact
    avg_price = x_eff * (x_eff + q) / K
    return avg_price

```

### 5.1 Exit fill — тэгш хэмтэй БИШ

Гарахдаа өөрийн зарах impact-ыг заавал тооцно. Гэхдээ нэг нөхцөлт байдал ил байх ёстой:

Exit trigger нь **урсгал эргэх мөчид** ажиллана, өөрөөр хэлбэл гарах latency-ийн үеийн V нь **нөхцөлт байдлаар сөрөг** — чамаас өмнө бусад нь зарж амжина. Тиймээс:

1. **Exit дүрмийг урьдчилж тогтооно** (§12.4-ийн хэлбэрээр: `net_flow_3slot < 0` тодорхой хугацаанд, эсвэл `burst_age > A`, эсвэл хатуу stop).
2. Exit үнийг **тухайн мөчийн бодит ажигласан урсгалаар** бодно — entry-ийн тэгш хэмтэй таамаглалаар биш.
3. **Rug tail case тусад нь:** dev dump чиний өмнө land хийсэн нөхцөлд curve-ээс гарах бодит үнийг тусад нь тайлагнана. Энэ тоо нь Phase 4 шүүлтүүрийн false negative-ийн жинхэнэ өртгийг өгнө.

> **v1.3 тэмдэглэл:** exit дүрмийн параметр (урсгал эргэх хугацаа, насны хязгаар A) нь §7 3c-ийн hazard муруйн гаралт. Тиймээс `fwd_net_ret` нь extract-ийн үед тогтмол багана болж бодогдохгүй — `nf3_traj_75` траектороос **локал дээр**, Phase 3-ийн дараа бодогдоно. Энэ шалтгаанаар траектор extract-д ордог.

**Latency scenario-ууд (бүгдийг зэрэг ажиллуулна):**

- `L0`: 0ms — онолын дээд хязгаар, зөвхөн лавлагаа
- `L1`: 400ms (1 slot)
- `L2`: 1000ms
- `L3`: 3000ms

**Шимтгэл:** pump.fun 95bp + creator 30bp = 1.25% × 2 тал = 2.5% (эмпирик). Priority fee + tip-ийг тусад нь параметр болгож 0.001–0.01 SOL хооронд шалгана.

---

## 6. Дискиплин (ажиллуулахаас ӨМНӨ бичигдэнэ)

1. **Frozen holdout:** Хугацааны сүүлийн 30%-ыг тусгаарлаж, Phase 7 хүртэл **огт нээхгүй**. **v1.3 — татах үедээ салгана, дараа биш:**
  - `[2026-05-10, 2026-07-12)` launch = **dev**
  - `[2026-07-12, 2026-08-08)` launch = **holdout**
  - **Хоёр тусдаа query, хоёр тусдаа файл, өөр billing мөчлөг.** Holdout нь `data/holdout/` дор, pipeline-аас гадуур.
  - Holdout файл дээр Phase 7 хүртэл ямар ч тоолол, sanity, describe хийхгүй.
  - Хуваалт нь **токен үүссэн хугацаагаар**, event-ийн хугацаагаар биш — ингэснээр §8.2-ийн "нэг токений бүх event нэг талд" дүрэм автоматаар хангагдана.
2. **Pre-registration:** Phase бүрийн kill criterion-ыг ажиллуулахаас өмнө git-д commit хийнэ. Дараа өөрчилсөн бол commit history-д харагдана.
3. **Standard error:** Токен ба минутаар **double-cluster**. Энгийн t-test хориотой.
4. **Тайлагнал:** Fat tail тул median, 10% trimmed mean, bootstrap 95% CI гурвуулаа. Дан mean хориотой.
5. **Split-half:** Токенуудыг сондгой/тэгш ID-аар хувааж найдвартай байдлын дээд хязгаарыг гаргана. (Өдрөөр хуваахгүй — токен минутаар амьдардаг.)
6. **Placebo:** Идэвх, curve stage, **цаг хугацаагаар** таарсан санамсаргүй когорт. Гурвуулаа таарууллах ёстой.
7. **Тестийн бүртгэл:** Дата руу харсан таамаглал бүрийг `test_log.md`-д бүртгэж commit хийнэ — phase, нүд (босго × τ), огноо, үр дүн. Энэ нь p-hacking аудитыг субъектив таамаглалаас объектив тоолол болгоно: "хэдэн тест хийсэн бэ" гэдэгт git log хариулна.
8. **Дуугүй сонголт байхгүй (v1.3):** Spec-д заагаагүй конвенцийг хэрэгжүүлэлт сонгосон бол тэр сонголтыг `decisions.md`-д ил бүртгэж, дараагийн spec хувилбарт оруулна. Эс бөгөөс аудитад "яагаад ингэсэн" гэдэг нь зөвхөн кодоос уншигдана.

---

## 7. Phase-ууд, kill criterion-той

### Phase 0 — Дата болон нэгжийн шалгалт

**Гүйцэтгэл:** Claude Code **Гаралт:** ClickHouse table (burst мөр), шалгалтын тайлан **Шалгалтууд:**

- **1a. Reserve цувааны бүтэн байдал, хоёр тал** — `Δvtoken = ∓token_amount` бүх мөр дээр; `Δvsol = ±sol_amount` (mayhem бус)
- **1b. Constant product** — `|Δ(x·y)/(x·y)| < 1e-9`
- **1c. Эхний x_pre = тухайн токений createevent reserve** — яг таарах
- Curve-ээр бодсон үнэ vs бодит `sol_amount/token_amount` — таарах ёстой
- Universe нь launch-аар тодорхойлогдсон эсэх (migrate хийгээгүй токенууд байгаа эсэх)
- Өдрийн burst тоо тогтвортой эсэх (дата дутуу байгаа эсэх)
- **Fee handling — ШИЙДЭГДСЭН:** `sol_amount` **= NET.** Нотолгоо: mayhem бус 5,132,812 дараалсан хос дээр `Δvsol = ±sol_amount` **100.0000%** таарсан; gross таамаглал 1.47% дээр л таарсан. Мөн NET таамаглал дор токений эхний арилжаа **74,733/74,733** дээр `x₀ = 30 SOL`-ыг яг гаргасан.
- **§0.4 хасагдав:** cumulative нийлбэр байхгүй болсон тул хуримтлагдах floating point алдааны асуудал алга.

> **v1.3 — шалгалт 1a/1b/1c-ийн байршил.** burst-only архитектур дор түүхий event локал руу гарахгүй тул эдгээр шалгалтууд **Dune SQL дотор** ажиллаж, нэгтгэсэн тоо (таарсан мөрийн тоо, зөрүүний квантиль) буцаана. Энэ нь шалгалтыг сулруулахгүй — on-chain state-ийн дотоод уялдааг шалгасаар байна. Шалгалтын query нь extract-аас **тусдаа** ажиллана.

**KILL:** Curve reconstruction таарахгүй бол цааш явахгүй. Бүх зүйл үүн дээр тогтоно.

> Энэ хуваалт шалгалтыг сулруулахгүй, **ХҮЧТЭЙ** болгож байна: өөрсдийн арифметикийг биш, on-chain state-ийн дотоод уялдааг шалгаж, Dune-ийн decode-ыг бие даан баталж байна. mayhem токен дээр segment тус бүрд хэрэглэнэ. Эмпирик: mayhem бус хосууд дээр |Δ(x·y)/(x·y)| median 8.4e-12 — босгоос 3 дэс дотор.

---

### Phase 1 — Зардлын загвар (детерминистик)

**Гүйцэтгэл:** Claude Code **Гаралт:** `cost_model.py` + unit test-үүд **Агуулга:** §1.1, §5-ийн бүх томъёо (§5.1-ийн exit асимметр багтана) **Шалгалт:** Түүхэн бодит арилжаануудыг оруулаад, загварын гаргасан үнэ бодит гүйцэтгэсэн үнэтэй таарч байна уу. Зөрүү > 0.5% бол загварт алдаа бий.

**KILL:** Байхгүй. Энэ бол арифметик, ажиллах ёстой.

---

### Phase 2 — Overhead supply (детерминистик)

**Гүйцэтгэл:** Claude Code **Гаралт:** Burst бүр дээрх `OH(t)`, `OH_ratio(t)`, `OH_conc(t)`

> **v1.3:** OH нь extract-ийн үед Dune SQL дотор тооцогдож, `oh`, `oh_ratio`, `oh_conc`, `oh_n_wallets` багана болж ирнэ. SQL↔Python parity батлагдсан (88/88 мөр, 12 аравт, харьцангуй зөрүү max 1.6e-15). `src/oh_reference.py` нь SQL-ээс хамааралгүй лавлагаа хэвээр үлдэж, regression тестийн эрх мэдэл болно. Тиймээс Phase 2 нь шинэ тооцоолол биш, **баталгаажуулалт** болов.

**Хэрэгжүүлэлт (лавлагаа):** Токен бүрээр event-үүдийг дараалуулж, wallet → (tokens, cost_basis) dict-ийг incremental шинэчилнэ. **Шалгалт:** Σ tokens_held ≤ нийт нийлүүлэлт. OH ≥ 0 үргэлж. 0 ≤ OH_conc ≤ 1. OH = 0 бол OH_conc = 0.

**KILL:** Байхгүй.

---

### Phase 3 — ТООЛОЛТ (загваргүй) ⭐ ХАМГИЙН ЧУХАЛ

**Гүйцэтгэл:** Claude Code **Хугацаа:** 5–7 хоног **Энэ phase судалгааны амьдралыг шийднэ.**

> **v1.3:** Phase 3 бүхэлдээ **хөлдөөсөн локал файл** дээр ажиллана. Dune руу дахин хандахгүй. §3d-ийн 9 нүд, §3c-bis-ийн tercile, §3e-ийн 3 багц бүгд ижил extract дээр. Энэ нь "үр дүн харсны дараа дахин татах" гадаргууг бүтцээрээ хаана.

**3a. Baseline + power:**

- `fwd_net_flow(τ)`-ийн нөхцөлгүй тархалт (median, quantile-ууд)
- **Power analysis — 3b-ээс ӨМНӨ.** Baseline тархалтаас bootstrap-аар minimum detectable effect бод: одоогийн sample size, double-cluster SE-тэй нөхцөлд, 3-р зүйлийн эдийн засгийн босго хэмжээний d10−d1 зөрүүг илрүүлэх power ≥ 80% мөн үү?
- **Хэрэв power < 80%:** kill criterion-ий "CI тэгийг агуулна" гэдэг нь "сигнал байхгүй" биш "**дата хүрэлцэхгүй**" гэсэн үг. Тэр тохиолдолд universe-ээ томсгоно (илүү урт хугацаа) — судалгааг үхүүлэхгүй. Энэ ялгааг тайланд тодорхой заана.

**3b. Decile тест:** Бүх burst event-ийг `OH_ratio`-оор 10 бүлэг болгож, бүлэг бүрийн `fwd_net_flow(12slot)`-ийн median болон bootstrap CI гарга.

**3c. Hazard:** `P(burst dies at age a | alive at a)` — a = 1..75 slot. Тогтмол уу, өсдөг үү?

> **v1.3 — муруйн эхлэлийг уншихад заавал тооцох зүйл:** диагностик хэмжилтээр `death_age` median = 3.0 slot гарсан бөгөөд энэ нь цонхны механикаас гарч байна — median burst нь өөрийн slot цонхноос гарах мөчид "үхэж" байна. Мөн §4.1-ийн trigger (5-slot) ба §4.3-ийн alive (3-slot) хоорондын нэгжийн зөрүү нь эхний хэдэн slot-ийн hazard-д нөлөөлнө. Эдгээр нь урьдчилж бүртгэгдсэн бөгөөд label тодорхойлолт тэдгээрийг харсны дараа өөрчлөгдөөгүй.

**3c-bis. Stratified hazard (ЗААВАЛ):** Мөн hazard-ыг `OH_ratio`-ийн tercile-ээр гурав хувааж зэрэгцүүлнэ. Судалгааны гол таамаглал нь яг "ачаа их үед burst хурдан үхдэг" тул үүнийг шууд харах ёстой. Хэрэв өндөр-OH tercile-ийн hazard curve нь бага-OH-ээс системтэйгээр дээгүүр байвал энэ нь 3b-ийн decile тестээс **хамааралгүй, хоёр дахь бие даасан нотолгоо**. Загвар биш, тоолол хэвээр.

> Slot нарийвчлал нь §11-ийн Б хувилбарыг (1–2 slot цонх) шууд ялгах чадвартай — секундээр хэмжвэл тэр ялгаа харагдахгүй.

**3d. Sensitivity:** Burst босго 0.05x / 0.10x / 0.20x; τ = 5/12/37 slot. §4.1-ийн үндсэн нүд шийдвэр гаргана; эдгээр нь robustness. `OH_conc`-ийн double sort мөн энд. `nf3_traj_75_excl_pre` дээрх hazard-ийг мөн robustness болгож зэрэгцүүлнэ.

**3e. Rug-conditioned split (ЗААВАЛ):** Түүхэн дата дээр rug эсэх нь ex-post мэдэгдэнэ — Phase 4-ийн классификатор хэрэггүй, бодит үр дүнг label болгоно. 3b, 3c-г **гурван багц дээр давтана**:

- бүх токен
- rug-уудыг хассан "цэвэр" багц
- зөвхөн rug-ууд

Энэ нь хоёр зүйлийг зэрэг өгнө: (1) сигнал бохирдолгүй орчинд байгаа эсэх, (2) Phase 4-ийн шүүлтүүр хэр чухал болох. Хэрэв сигнал зөвхөн цэвэр багц дээр л гарвал → шүүлтүүр зайлшгүй. Хэрэв гурвуулаа ижил бол → шүүлтүүр зөвхөн алдагдал хамгаалагч, сигналын нөхцөл биш.

> **Дарааллын тайлбар:** Ажиллах үеийн дараалал (шүүлтүүр → урсгал → ачаа → зардал) болон судалгааны дараалал өөр. Судалгааг эрсдэлээр эрэмбэлдэг: хамгийн их магадлалтай үхлийн цэгийг хамгийн түрүүнд шалгана.

**KILL CRITERION (урьдчилж бичигдсэн, хоёр нөхцөлтэй):**

> **(i) Статистик:** Үндсэн нүд дээр (0.10x, τ=12 slot) decile 10 ба decile 1-ийн `fwd_net_flow(12slot)` median-ийн зөрүүний bootstrap 95% CI нь тэгийг агуулж байвал → `OH_ratio` нь signal биш. Түүнийг хасаад f1–f5 дээр давтана. Тэдгээр нь ч мөн адил бол → **бүх санаа зогсоно.**
>
> **(ii) Эдийн засаг:** CI тэгийг агуулаагүй ч, decile 10-ийн median `fwd_net_flow(12slot)`-ээс гарах үнийн хөдөлгөөн `(1 + ΔV/x)² − 1` нь лавлагаа нөхцөл дэх BE-ээс (x=50, q=1 SOL, L2 latency, fees=0.025) **бага** бол → сигнал статистикийн хувьд бодит ч ашиглагдахааргүй. Phase 4–6-д гурван долоо хоног зарцуулаад Phase 7 дээр үхэхийн оронд энд зогсоно.
>
> Хоёулаа давсан тохиолдолд л цааш явна.
>
> **Үл хамаарах зүйл:** 3a-ийн power < 80% бол (i)-ийн FAIL нь kill биш — дата нэмэх шийдвэр.

**Hazard-ийн тайлбар:**

- Тогтмол hazard → "хоцрох" гэсэн ойлголт байхгүй → аль ч мөчид орох нь адил → **хурдны уралдаан биш**
- Өсөх hazard → хоцрох нь бодит → орох цонхны өргөнийг тоогоор гаргана
- Энэ тоо Phase 8 ба §12.4-ийн гарах хугацааны хязгаарыг шууд өгнө

---

### Phase 4 — Rug filter (давтагдсан ажил)

**Гүйцэтгэл:** Claude Code **Лавлагаа:** MemeTrans (arXiv 2602.13480) **Label:** Rug/high-risk эсэх (dev dump, LP татагдсан, эсвэл launch-аас хойш N цагт бүрэн үхсэн)

> **v1.3:** энэ Phase-ийн feature-ууд burst мөрөнд байхгүй — тусдаа Dune таталт шаардана. Түүний зардлыг Phase 3 эерэг гарсны дараа тооцно.

**Feature бүлгүүд:**

1. *Эзэмшлийн төвлөрөл* — launch-ийн дараах эхний N минут дахь Gini / HHI / дээд-10 хаягийн эзлэх хувь
2. *Санхүүжилтийн graph* — олон хаяг нэг эх сурвалжаас SOL авсан эсэх; connected component-оор "жинхэнэ оролцогчийн тоо"
3. *Wash trading шинж* — хаягийн buy/sell эргэлтийн харьцаа, арилжааны хэмжээний давтагдал, хугацааны зайн жигд байдал
4. *Dev-ийн зан төлөв* — үүсгэгчийн эзэмшил, түүний өмнөх токенуудын түүх

**Загвар:** Logistic regression → gradient boosting. Neural network битгий, тайлбарлагдах чанар нь илүү үнэтэй. **Хэмжүүр:** Precision @ өндөр recall. False negative (rug өнгөрөх) нь false positive-ээс хамаагүй үнэтэй. Өртгийг §5.1.3-ийн tail тоогоор илэрхийлнэ.

**KILL:** AUC < 0.65 бол feature-үүдээ дахин үзнэ. MemeTrans-ийн үр дүнг давтаж чадахгүй бол хэрэгжүүлэлтэд алдаа бий.

---

### Phase 5 — Нөхцөлт загвар

**Гүйцэтгэл:** Claude Code **Урьдчилсан нөхцөл:** Phase 3 эерэг (хоёр нөхцөл хоёулаа) **Загвар:** `P(fwd_net_flow(τ) > threshold | f1..f11)` — logistic regression эхлээд, дараа нь gradient boosting **Validation:** Time-series walk-forward. Random split **хатуу хориотой**.

**Embargo (ЗААВАЛ):** Feature нь 25 slot хойшоо, label нь 37 slot урагшаа хардаг тул split хилийн ойролцоох event-үүд хоёр талд давхцсан мэдээлэл агуулна. Train/test хооронд дор хаяж `max(feature цонх 25 slot) + max(τ 37 slot)` = **62 slot embargo** тавина. Мөн "нэг токений бүх event нэг талд" гэсэн дүрмийг split кодын дотор **assertion** болгоно (§8.2-ийн unit test шаардлагатай давхардана — санаатай).

> **v1.3:** dev/holdout хуваалт нь татах үедээ хийгдсэн (§6.1) тул Phase 5-ийн walk-forward split нь **dev файлын дотор** ажиллана. Holdout нь Phase 7 хүртэл хөндөгдөхгүй.

**Тайлагнал:** Decile бүрийн бодит fwd_net_flow, calibration plot, feature importance

**KILL:** Out-of-sample дээр decile 10 vs decile 1 ялгаа алга бол → загвар in-sample noise барьсан.

---

### Phase 6 — Wallet cluster (бүдүүн туг)

**Гүйцэтгэл:** Claude Code **Урьдчилсан нөхцөл:** Phase 5 эерэг **Хэрэгжүүлэлт:**

1. Санхүүжилтийн graph бариx: хаяг A хаяг B-д анх SOL илгээсэн → ирмэг
2. Connected component = cluster
3. `cluster_seen` (f12) тугийг тооцох **Тест:** Phase 5-ийн загварт f12 нэмэхэд out-of-sample AUC/decile spread **сайжирч байна уу**

**Механик confound-ийн засвар:** Cluster-ийн "чанар"-ыг үнэлэхдээ тухайн арилжааны өөрийнх нь curve impact-ыг хассан үнэ дээр markout бод. Мөн арилжааны хэмжээгээр нөхцөлдүүл — эс бөгөөс "мэдээлэлтэй" биш "том" гэдгийг л хэмжинэ.

**KILL:** Сайжруулалт байхгүй бол f12-ыг хая. **Энэ нь бүх санааг үхүүлэхгүй** — бүдүүн туг сул хэмжигдэхүүн тул сигнал байсан ч барихгүй өнгөрч болно. "Дараа буцаж ирнэ" гэж тэмдэглээд цааш.

---

### Phase 7 — Эдийн засгийн тест (holdout нээгдэнэ) 🔓

**Гүйцэтгэл:** Claude Code **Энэ бол holdout-ыг нээх ганц цэг.**

Phase 5/6-ийн загварын дээд decile дээр §5-ийн бүрэн зардлын загвараар (§5.1-ийн exit асимметр багтаасан) `fwd_net_ret` бод — L1/L2/L3 latency scenario тус бүрд.

**Тайлагнал:**

- Latency scenario бүрийн expectancy (median, trimmed mean, bootstrap CI)
- Ашгийн төвлөрөл: дээд 1%, 5% арилжаа нийт ашгийн хэдэн хувийг үүсгэсэн
- Ямар curve depth (x) дээр expectancy эерэг байна → §12.3-ийн босгыг өгнө
- Позицийн хэмжээ q-ийн мэдрэмж: q = 0.5, 1, 2, 5 SOL → §12.3-ийн slippage хязгаарыг өгнө
- Exit fill-ийн бодит зардал vs тэгш хэмтэй таамаглал (зөрүү хэдэн %)

**KILL CRITERION:**

> Аль ч бодитой latency scenario (L1–L3) дээр дээд decile-ийн expectancy эерэг биш бол → **систем эдийн засгийн хувьд ажиллахгүй.** Загвар статистикийн хувьд ажиллаж байсан ч энд зогсоно.

**Хэрэв зөвхөн L0/L1 дээр эерэг бол** → хурдны уралдаан. Чиний ялах магадлал бага. Энэ нь бас хүчинтэй, үнэ цэнтэй хариулт. §12-ийн систем энэ тохиолдолд **баригдахгүй.**

---

### Phase 8 — Paper trading

**Урьдчилсан нөхцөл:** Phase 7 эерэг **Хэрэгжүүлэлт:** §12-ийн бүрэн систем real-time сигнал үүсгэж бичнэ, гүйцэтгэхгүй. Дараа нь бодит үнэтэй тулгана. **Хугацаа:** Дор хаяж 4 долоо хоног, 100+ сигнал **Хэмжих зүйл:** Backtest-ийн таамагласан entry үнэ vs бодит үнэ. Зөрүү > 30% бол fill model буруу.

Зөвхөн энэ давсны дараа бодит капитал, бас маш жижиг хэмжээгээр.

---

## 8. Багажийн үүрэг хуваарилалт

**Гурван багаж:** Claude (chat) — судалгааны удирдагч, Claude Code — гүйцэтгэл, ChatGPT — судалгааны аудит.

### 8.1 Claude (chat) — Судалгааны удирдагч

- Phase бүрийн спецификаци, kill criterion боловсруулах
- Үр дүнг тайлбарлах, дараагийн алхмыг шийдэх
- ChatGPT-ийн аудитын дүгнэлтийг үнэлэх (бүх шүүмж зөв байх албагүй)
- Kill эсэх шийдвэрийг гаргах

### 8.2 Claude Code — Гүйцэтгэл

**Prompt template:**

```
Solana bonding-curve flow research, Phase <N>-ийг хэрэгжүүл.

Хавсаргасан spec v1.3-ийн §<холбогдох хэсгүүд>-ийг уншаад:

1. Дараах файлуудыг бич: <жагсаалт>
2. Бүх feature backward-looking байх ёстой. t мөчид block_time > t
   өгөгдөл ашиглах нь bug — тэр шалгалтыг unit test болгож бич.
3. Standard error-ийг токен болон минутаар double-cluster хийж бод.
4. Үр дүнг median + 10% trimmed mean + bootstrap 95% CI гурвуулаар
   тайлагна. Дан mean битгий гарга.
5. Kill criterion-ий үр дүнг тодорхой гарга: PASS эсвэл FAIL.
   Phase 3 бол статистик (i) ба эдийн засаг (ii) хоёрыг тусад нь.
6. Ажиллуулахын өмнө kill criterion-ыг commit хий.
7. Дата руу харсан тест бүрийг test_log.md-д бүртгэ (§6.7).
8. Temporal leakage-ийн шалгалтыг ХОЁР ХЭЛБЭРЭЭР заавал бич:

   (а) СИНТЕТИК UNIT TEST — t мөчид тооцогдсон feature бүр дээр,
       block_time > t өгөгдөл ашиглагдаагүйг синтетик датагаар
       батал (ирээдүйн утгыг зориуд гажуудуулаад feature
       өөрчлөгдөхгүй байхыг шалга). Мөн эсрэг тест: §4.2-ийн
       label-ууд тэр гажуудалд ЗААВАЛ хариу үзүүлэх ёстой.
   (б) SQL↔PYTHON PARITY — Dune SQL-ийн гаралтыг SQL-ээс
       хамааралгүй Python лавлагаатай элемент тутам харьцуул.

   ХОЁУЛАА ШААРДЛАГАТАЙ. Parity нь "хоёр тал ижил" гэдгийг,
   синтетик тест нь "тэр ижил нь ЗӨВ" гэдгийг батална. Зөвхөн
   parity бичвэл хоёр тал давтсан алдаа өнгөрнө — Phase 0-д
   f3/f8/f9 дээр яг тэр тохиолдол гарсан.

   Мөн cost basis-ийн шинэчлэлтийг: sell гарахад cost basis
   өөрчлөгдөхгүй, дахин худалдан авалт зөв дундажлагдахыг
   unit test-ээр батал.
   Мөн train/test split-ийг unit test-ээр батал: split цаг
   хугацааны дарааллаар хийгдсэн, нэг токений event хоёр тал
   руу хуваагдаагүй, embargo хангагдсан.
   Slot ordering: нэг slot доторх event-үүд tx_index, дараа нь
   ix_index-ээр зөв дараалсныг батал. Slot хилийн off-by-one-ыг
   тусад нь шалга — §3-ийн мөрийн түвшний хил ба §4.2-ийн
   label-ийн хил хоёр зөв ажиллаж байгааг нэг синтетик кейс
   дээр зэрэг батал.

Хэрэв spec тодорхойгүй хэсэгтэй бол таамаглаж бүү бич — асуу.

```

### 8.3 ChatGPT — Судалгааны аудит

**Оруулах:** spec + тухайн Phase-ийн үр дүн + код. **Оруулахгүй:** судалгааны удирдагчийн тайлбар, хүлээлт, чатын түүх. Аудитор хүлээлтийг мэдвэл түүнийг дэмжих хандлагатай болно.

**Prompt template:**

```
Чи бол шүүмжлэгч аудитор. Доорх судалгааны дизайн болон үр дүнг уншаад,
энэ судалгаа өөрийгөө хуурч байгаа бүх зам мөрийг ол.

[spec + Phase-ийн үр дүн + код]

Тусгайлан шалга:

1. LOOKAHEAD BIAS. Аль feature ирээдүйн мэдээлэл агуулж байна вэ?
   Цонхны хил (inclusive/exclusive) зөв үү? Slot доторх дараалал
   зөв тооцогдсон уу? Label болон feature-ийн хугацаа давхцаж
   байна уу? Walk-forward embargo хангалттай юу?

2. SURVIVORSHIP. Universe launch дээр тодорхойлогдсон уу, migration
   дээр үү? Үхсэн токенууд дата дотор байна уу? Burst-only
   архитектур нь идэвхээс хамаарсан шүүлт оруулсан уу?

3. P-HACKING. Хэдэн таамаглал шалгасан бэ (test_log.md-тэй тулга)?
   Үндсэн нүд урьдчилж зарлагдсан уу? Босго, цонхны сонголт үр дүнг
   харсны дараа өөрчлөгдсөн үү? (git history шалга)

4. SE ба ТАРХАЛТ. Кластерлалт зөв үү? Fat tail дээр mean ашигласан
   уу? Bootstrap зөв хийгдсэн үү? Power хангалттай юу — null үр дүн
   "сигнал байхгүй" мөн үү, эсвэл "дата хүрэлцэхгүй" юү?

5. FILL ASSUMPTION. Backtest-ийн entry үнэ бодитой юу? Өөрийн impact
   тооцогдсон уу? Exit дээр урсгалын нөхцөлт сөрөг чанар тооцогдсон
   уу? Latency зөв загварчлагдсан уу?

6. МЕХАНИК CONFOUND. "Мэдээлэлтэй" болон "том/дагагдсан" хоёрыг
   ялгаж чадаж байна уу? Илэрсэн хамаарал нь шалтгаан уу, эсвэл
   AMM-ийн механикаас автоматаар гарч байна уу?

7. ХАМГИЙН ЧУХАЛ: Энэ үр дүн ХУДАЛ байх хамгийн үнэмшилтэй
   тайлбар юу вэ? Тэр тайлбарыг хэрхэн шалгах вэ?

Сайшаал битгий бич. Зөвхөн сул талыг ол. Юу ч олдохгүй бол тэгж хэл.

```

---

## 9. Хугацааны төлөвлөгөө


| Phase | Ажил                              | Хугацаа       | Хуримтлагдсан         |
| ----- | --------------------------------- | ------------- | --------------------- |
| 0     | Дата + шалгалт                    | 4–7 хоног     | 1 долоо хоног         |
| 1     | Зардлын загвар                    | 2 хоног       | 1.5                   |
| 2     | Overhead supply (баталгаажуулалт) | 1 хоног       | 1.5                   |
| **3** | **Тоолол ⭐**                      | **5–7 хоног** | **3–3.5 долоо хоног** |
| 4     | Rug filter                        | 5 хоног       | 4.5                   |
| 5     | Нөхцөлт загвар                    | 7 хоног       | 6                     |
| 6     | Cluster туг                       | 5 хоног       | 7                     |
| 7     | Эдийн засгийн тест                | 3 хоног       | 7.5 долоо хоног       |
| 8     | Paper trading                     | 4 долоо хоног | 11.5 долоо хоног      |


**Phase 3 дуусахад (~3.5 долоо хоног) чи энэ судалгаа амьд эсэхийг мэднэ.** Ихэнх эрсдэл тэнд шийдэгдэнэ.

---

## 10. Зардал


| Phase | Хэрэгтэй зүйл                      | Зардал                   |
| ----- | ---------------------------------- | ------------------------ |
| 0–7   | Dune Free, локал машин, ClickHouse | **$0**                   |
| 8     | Тасралтгүй ажиллах VPS             | ~$20–40/сар              |
| Дараа | gRPC, colocation                   | Зөвхөн Phase 7 эерэг бол |


**Дүрэм:** Мөнгө зарцуулах бүх зүйл судалгааны эерэг үр дүнгийн ДАРАА ирнэ. Сөрөг үр дүн гарвал алдсан зүйл бараг байхгүй.

> **v1.3:** burst-only архитектур нь Phase 0–7-ийг Dune Free дээр бүрэн universe-тэйгээр ажиллуулах боломж өгсөн. Dune Plus ($349/сар) болон бусад эх сурвалж (Helius, Flipside, Allium) нь **шаардлагагүй** болсон. Хэрэв ирээдүйд universe-ийг өргөтгөх шаардлага гарвал (§7 Phase 3a-ийн power < 80%) тэр асуулт дахин нээгдэнэ.

---

## 11. Ямар үр дүн ч ялалт

Энэ судалгаа гурван боломжит хариулттай, гурвуулаа үнэ цэнтэй:

**А. Эерэг, өргөн цонхтой** → тоглож болох тоглоом. Phase 8 руу.

**Б. Эерэг, гэхдээ зөвхөн 1–2 slot** → хурдны уралдаан. Оролцохгүй байх нь зөв шийдвэр. Судалгаа өөрөө үнэ цэнтэй хэвээр.

**В. Сөрөг** → AMM дээр богино хугацааны урсгал үргэлжлэхгүй гэдгийг зөв аргачлалаар няцаасан. Энэ бол нийтлэгдээгүй үр дүн бөгөөд portfolio-д тавихад бүрэн хүчинтэй ажил.

Гурвуулаа мастерийн хөтөлбөрт авч очих боломжтой. Ганцхан муу үр дүн бол дуусгаагүй судалгаа.

---

## 12. Ажиллах үеийн систем (Phase 7 эерэг тохиолдолд)

**Энэ хэсэг нөхцөлт.** Тоонууд нь Phase 5 ба 7-ийн гаралт — одоо зөвхөн бүтэц тодорхойлогдож байна. Phase 7 сөрөг бол энэ хэсэг хэрэгжихгүй.

**Ажиллах дараалал нь судалгааны дараалалтай эсрэг:** судалгаанд хамгийн эргэлзээтэйг эхэлж шалгадаг; ажиллахдаа хамгийн хямд шүүлтүүрийг эхэлж тавина.

### 12.1 Давхарга 0 — Урьдчилсан шүүлт (launch дээр, нэг удаа)

Phase 4-ийн rug классификатор шинэ токен бүр дээр launch-ийн дараа 30–60 секундэд ажиллаж "хяналтын жагсаалтад авах уу" гэдгийг шийднэ. Real-time биш тул хэдэн зуун ms зарцуулж болно. Ихэнх токен энд хасагдана.

### 12.2 Давхарга 1 — Burst илрүүлэлт (тасралтгүй)

Хяналтын жагсаалт дахь токенуудын урсгалыг үргэлжлүүлэн уншиж, §4.1-ийн burst нөхцөл хангагдсан агшинд л дараагийн давхарга руу дамжуулна. Энэ бол trigger, шийдвэр биш.

### 12.3 Давхарга 2–3 — Шийдвэрийн дүрэм

Загварын магадлал өндөр байгаа нь **авах шалтгаан биш**. Шийдвэр нь харьцуулалт:

```
E[ашиг | нөхцөл]  >  BE(x, q, V̂, fees)

```

Баруун тал нь §1.1-ийн breakeven, тухайн агшны curve depth `x`, хэмжээ `q`, хоцролтын үеийн хүлээгдэж буй урсгал `V̂`-аар бодогдоно.

**Гол цэг:** ижил магадлал өөр `x` дээр өөр шийдвэр өгнө. Гүехэн curve дээр slippage их тул өндөр магадлал ч хүрэлцэхгүй; гүн дээр бага магадлал хангалттай байж болно. Тиймээс систем "магадлал > 0.7 бол ав" гэж ажиллахгүй — босго нь агшин бүрд curve state-ээс тооцогдоно. Phase 7-ийн depth-шинжилгээ энэ босгыг өгнө.

**Хэмжээ** `q` **— хоёр хязгаарлалт зэрэг:**

- *Slippage хязгаар:* `q/x` тодорхой хувиас хэтрэхгүй (Phase 7-ийн q-мэдрэмжээс). Гүехэн curve дээр өөрөө өөрийгөө хөөж авна.
- *Магадлалын жин:* загварын итгэлээс хамаарч хэмжээ өөрчлөгдөнө, гэхдээ бүрэн Kelly биш — fat tail, загварын алдааны тодорхойгүй байдал тул фракциональ (≈¼ Kelly).

### 12.4 Гарах дүрэм — урьдчилж, өөрчлөгдөхгүй

Орохоос **өмнө** тогтоогдож, гараар өөрчлөгдөхгүй:

- Урсгал эргэвэл (`net_flow_3slot < 0` тодорхой хугацаанд) → гарна
- Насны хязгаар (Phase 3c/3c-bis-ийн hazard муруйнаас гарсан тоо) → гарна
- Хатуу stop: тодорхой хувийн алдагдал → гарна

Hazard тогтмол бол "хүлээх нь тус болохгүй" → богино хугацаа. Өсдөг бол оновчтой хугацаа тооцогдоно.

### 12.5 Амьд байхад хянах зүйлс

1. **Fill зөрүү:** таамагласан entry үнэ vs бодит (Phase 8-ийн 30%-ийн босго амьд үед ч хүчинтэй)
2. **Calibration drift:** загвар 0.7 гэсэн үед бодитоор 0.7 орчим гарч байна уу
3. **Realized vs expected expectancy:** rolling цонхоор; зөрүү статистикийн хувьд бодит болвол → **автоматаар зогсоно**

Гурав дахь нь зайлшгүй. Ямар ч edge мөнхийн биш — pump.fun-ий дүрэм өөрчлөгдөж болно, бусад оролцогчид ижил зүйлийг олж болно. Автомат зогсолтгүй систем нь edge алга болсны дараа ч арилжаагаа үргэлжлүүлнэ.

---

## 13. Зах зээлийн бүтцийн тухай тэмдэглэл

Энэ зах зээл CFD/futures-ээс бүтцээрээ ялгаатай бөгөөд тэр ялгаа нь давуу тал ба өртөг хоёуланг өгнө.

**Давуу тал:** нэг дүрэм олон мянган бүтээгдэхүүн дээр зэрэг ажиллана → дээж хурдан хуримтлагдана. Futures дээр жилээр хүлээх зүйлийг долоо хоногоор мэднэ. Мөн оролцогчид ихэвчлэн хүн эсвэл энгийн bot — HFT фирмүүд биш.

**Өртөг:**

1. **Олон дээж ≠ олон бие даасан дээж.** Токенууд нэгэн зэрэг халж, нэгэн зэрэг хүйтэрдэг. 5000 ажиглалт нь 200 "халуун цонх"-д багтаж байвал жинхэнэ бие даасан дээжийн тоо 200-д ойр. §6.3-ийн double-cluster яг үүнийг хэмжинэ — техник нарийн ширийн зүйл биш.
  > **Эмпирик (v1.3):** 3 хоногийн cohort дээр mayhem-бус 36,069 burst нь 4,593 ялгаатай минутад тархсан (launch цонх = 4,320 минут). 100% түүвэр дээр минутын кластер бараг ханасан. 90 хоногт минутын дээд хязгаар 129,600 — double-cluster нь одоогоор хязгаарлагч биш.
2. **Бүтээгдэхүүн бүр богино насалдаг** тул тухайн токенд тохируулга хийх боломжгүй. Дүрэм бүх токенд нийтлэг байх ёстой.
3. **Чадавхи хязгаартай.** q нь curve гүнээр хязгаарлагдана (x=50 үед 2 SOL нь 4% slippage). "Олон дээр жижгээр" гэдэг нь сонголт биш, албадлага.

**Мөн чанар:** futures дээр edge нь нэг үйл явцын *гүнд*, энд олон үйл явцын *өргөнд* байдаг. Тиймээс судалгаа нь cross-section — OH_ratio-г x-д хэвийлэх шаардлага үүнээс гарч байгаа юм.

**Ижил зүйл:** зардлыг давсан edge байх шаардлага. pump.fun-ий 2.5% round-trip нь futures-ийнхээс өндөр. Олон дээж тэр саадыг давахад тус болохгүй — зөвхөн **хариултаа хурдан мэдэхэд** тус болно.

