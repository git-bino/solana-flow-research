# Solana Bonding-Curve Flow Continuation Study
## Судалгааны бүрэн спецификаци ба гүйцэтгэлийн prompt-ууд

**Хувилбар:** v1.1
**Судалгааны удирдагч:** Claude (chat)
**Гүйцэтгэл:** Claude Code
**Аудит:** ChatGPT
**Код хяналт:** Codex

**v1.0 → v1.1 өөрчлөлт:** §3 (f7b), §4.1 (үндсэн нүд), §5 (exit fill), §6 (тестийн бүртгэл), §7 Phase 0 (fee handling, дата хэмжээ), Phase 3 (power analysis, эдийн засгийн босго, stratified hazard), Phase 5 (embargo), §12 (ажиллах үеийн систем — шинэ).

---

## 0. Судалгааны асуулт

**Үндсэн асуулт:**

> Migration хийгээгүй pump.fun bonding curve дээр, t хүртэлх ажигласан урсгалыг мэдэх нь дараагийн τ хугацааны цэвэр урсгалын нөхцөлт тархалтыг өөрчилдөг үү?

**H₀ (null):** Нөхцөлт тархалт = нөхцөлгүй тархалт. Ажигласан урсгал ирээдүйн урсгалын талаар мэдээлэл агуулаагүй.

**H₁:** Тархалт өөрчлөгддөг, бөгөөд тэр өөрчлөлт нь зардлыг давахуйц хэмжээтэй.

**Чухал:** Чиглэлийг урьдчилж заахгүй. "Их ачаа = аюултай" болон "их ачаа = хүчтэй момент" хоёулаа боломжит хариулт. Дата шийднэ.

**Хамрах хүрээ:** Зөвхөн pre-migration (bonding curve) үе шат. Post-migration AMM бол тусдаа судалгаа.

---

## 1. Математик суурь (тогтмол, маргаангүй)

### 1.1 Bonding curve

pump.fun-ий curve нь virtual reserve-тэй constant product:

```
x · y = k
x₀ = 30 SOL          (virtual SOL reserve)
y₀ = 1,073,000,191   (virtual token reserve)
k  = x₀ · y₀
```

`x` = 30 + (хуримтлагдсан бодит SOL). Migration threshold ≈ 85 бодит SOL → x ≈ 115.

**Spot үнэ:** `P(x) = x² / k`

**q SOL-ын худалдан авалт:**
```
Δy = y − k/(x+q)
дундаж төлсөн үнэ = x(x+q) / k
```

**Өөрийн slippage (яг):**
```
slip(q, x) = q / x
```

**Хоцролтын зардал** (чамаас өмнө V SOL land хийвэл):
```
lat(V, x) = (1 + V/x)² − 1
```

**Round-trip breakeven:**
```
BE = (1+V/x)² · (1+q/x)² · (1+fees) − 1
fees ≈ 0.02  (pump.fun 1% × 2 тал)
```

### 1.2 Overhead supply (ачаа)

Curve дээр үнэ детерминистик тул **эзэмшигч бүрийн орох үнэ яг мэдэгдэнэ**. Энэ нь ямар ч order-book зах зээлд байхгүй давуу тал.

Wallet w-ийн cost basis:
```
cb(w) = Σ(SOL_in) / Σ(tokens_received)   [FIFO биш, weighted average]
```
Sell гарвал tokens_received-ээс хасаж, cost basis-ийг хэвээр үлдээнэ.

**Ачааны хэмжигдэхүүн (үндсэн feature):**
```
OH(t) = Σ_{w: cb(w) < P(t)} [ tokens_held(w) × (P(t) − cb(w)) ]
```
Энэ нь SOL-оор илэрхийлэгдсэн, одоогийн үнээр бодогдсон **биелээгүй ашгийн нийт хэмжээ**.

**Заавал хэвийлэх:**
```
OH_ratio(t) = OH(t) / x(t)
```
Дан тоо токенууд хооронд харьцуулагдахгүй.

**Төвлөрөл (хоёрдогч):**
```
OH_conc(t) = дээд 3 wallet-ийн OH-д эзлэх хувь
```
Учир нь ижил OH_ratio-той хоёр токен өөр эрсдэлтэй: биелээгүй ашиг нэг whale дээр төвлөрсөн бол ганц транзакцаар бүхэлдээ буух боломжтой; тархсан бол зарах шийдвэрүүд корреляцгүй.

---

## 2. Дата

### 2.1 Эх сурвалж (эрэмбээр)

1. **Dune Analytics** — Solana decoded tables (pump.fun bonding curve buy/sell). Эхний сонголт.
2. **BigQuery Solana public dataset** — MemeTrans үүн дээр баригдсан, давтахад ашиглана.
3. Зөвхөн 1, 2 хангалтгүй бол Yellowstone gRPC өөрөө бичих. **Phase 0-д битгий эхэл.**

### 2.2 Universe (survivorship-ээс хамгаалах)

**ЧУХАЛ:** Universe нь *launch* дээр тодорхойлогдоно, *migration* дээр биш.

```
Universe = [T_start, T_end] хооронд үүсгэгдсэн БҮХ pump.fun токен
```
Migrate хийсэн эсэх нь **label**, шүүлт биш. Migrate хийсэн токенууд дээр л судалгаа хийвэл survivorship bias шууд орно.

Хугацаа: дор хаяж 90 хоног. Сүүлийн 30% нь **frozen holdout** (§6).

**Хэмжээ багасгах шаардлага гарвал (v1.1):** Зөвхөн **токеноор санамсаргүй түүвэрлэх** — launch бүрийн mint hash-аар тогтмол хувь (жишээ нь 20%) сонгоно. Идэвх, эзлэхүүн, наслалт, migration-аар шүүхийг **хатуу хориглоно** — тэдгээр нь survivorship-ыг дахин оруулна. Түүврийн хувь нь Phase 0-д тогтоогдож, дараа өөрчлөгдөхгүй.

### 2.3 Event schema

```sql
event(
  token_mint     TEXT,
  slot           BIGINT,
  block_time     TIMESTAMP,
  tx_index       INT,        -- slot доторх дараалал
  wallet         TEXT,
  side           ENUM('buy','sell'),
  sol_amount     DOUBLE,     -- бодит SOL (шимтгэлгүй)
  token_amount   DOUBLE,
  x_pre          DOUBLE,     -- арилжааны өмнөх virtual SOL reserve
  x_post         DOUBLE
)
```

`x_pre`, `x_post` -ийг curve state-ээс сэргээнэ. Тэдгээрийг **дахин тооцоолж, on-chain дүнтэй тулгаж шалгах** (§7 шалгалт).

### 2.4 Storage

ClickHouse (local). `ORDER BY (token_mint, slot, tx_index)`. Sanity: нийт токен тоо, өдрийн event тоо, x_post ба дараагийн x_pre-ийн залгаа.

---

## 3. Feature-ууд (бүгд backward-looking)

**Хатуу дүрэм:** t мөчид тооцогдох feature нь зөвхөн `block_time ≤ t` датаг ашиглана. Ямар ч ирээдүйн мэдээлэл орж болохгүй. Codex-ийн хяналтын гол зүйл (§8).

| ID | Нэр | Тодорхойлолт |
|---|---|---|
| f1 | net_flow_2s / 5s / 10s | Trailing цонх дахь (buy SOL − sell SOL) |
| f2 | accel | net_flow_2s / (net_flow_10s / 5) |
| f3 | n_buyers_5s | Trailing 5s дахь ялгаатай худалдан авагч хаягийн тоо |
| f4 | depth | Одоогийн x |
| f5 | curve_progress | (x − 30) / 85 |
| f6 | burst_age | Burst эхэлснээс хойших секунд (§4.1) |
| f7 | **OH_ratio** | §1.2 — гол шинэ feature |
| f7b | OH_conc | §1.2 — **зөвхөн robustness.** Phase 3-ийн амьд/үхэх шийдвэрт оролцохгүй |
| f8 | size_cv | Trailing 10s дахь арилжааны хэмжээний CV |
| f9 | round_frac | Дугуй тоотой (0.1/0.5/1.0 SOL) арилжааны эзлэх хувь |
| f10 | mkt_active_bursts | Тэр минутад burst-тэй байгаа токений тоо (сүлжээ даяар) |
| f11 | mkt_total_flow | Тэр минутын pump.fun-ий нийт SOL урсгал |
| f12 | cluster_seen | **Phase 6 хүртэл битгий тооц.** Bool: оролцож буй cluster-ийн аль нэг нь өмнө өссөн токен дээр гарч байсан уу |

---

## 4. Label-ууд

### 4.1 Burst тодорхойлолт

```
burst_start(t): net_flow_2s(t) ≥ max(3 SOL, 0.10 × x(t))
                AND түүнээс өмнөх 10s-д burst идэвхтэй байгаагүй
```

**ҮНДСЭН НҮД (v1.1, урьдчилж зарлагдсан):**
```
босго = 0.10 × x,  τ = 5s
```
Амьд/үхэх шийдвэр **зөвхөн энэ нүд дээр** гарна. Бусад босго (0.05x, 0.20x) ба τ (2s, 15s) нь robustness шалгалт: тэдгээрийн чиглэл үндсэн нүдтэй зөрвөл сэрэмжлүүлэг болно, гэхдээ үндсэн нүдийг орлохгүй. Энэ дүрэм нь 9 нүдийн аль нэгэнд санамсаргүй significant гарахад судалгаа "амьд үлдэх" боломжийг хаана.

### 4.2 Forward label-ууд

τ ∈ {2s, 5s, 15s} тус бүрд:

```
fwd_net_flow(τ)  = дараагийн τ дахь (buy SOL − sell SOL)
fwd_price_ret(τ) = P(t+τ)/P(t) − 1                    [raw, гүйцэтгэлгүй]
fwd_net_ret(τ)   = §5-ийн зардлын загвараар бодсон    [гол эдийн засгийн label]
```

### 4.3 Hazard label

```
burst_alive(a): burst эхэлснээс a секундын дараа net_flow_1s > 0 хэвээр үү
```

---

## 5. Зардлын загвар (fill model)

**Энэ бол судалгааны амин чухал хэсэг.** Fill assumption буруу бол бүх үр дүн утгагүй.

```python
def realistic_entry_price(x_at_signal, q, latency_ms, observed_flow_during_latency):
    # 1. latency хугацаанд land хийсэн бусдын урсгал
    V = observed_flow_during_latency
    x_eff = x_at_signal + V
    # 2. өөрийн impact
    avg_price = x_eff * (x_eff + q) / K
    return avg_price
```

### 5.1 Exit fill — тэгш хэмтэй БИШ (v1.1)

Гарахдаа өөрийн зарах impact-ыг заавал тооцно. Гэхдээ нэг нөхцөлт байдал ил байх ёстой:

Exit trigger нь **урсгал эргэх мөчид** ажиллана, өөрөөр хэлбэл гарах latency-ийн үеийн V нь **нөхцөлт байдлаар сөрөг** — чамаас өмнө бусад нь зарж амжина. Тиймээс:

1. **Exit дүрмийг урьдчилж тогтооно** (§12.4-ийн хэлбэрээр: `net_flow_1s < 0` тодорхой хугацаанд, эсвэл `burst_age > A`, эсвэл хатуу stop).
2. Exit үнийг **тухайн мөчийн бодит ажигласан урсгалаар** бодно — entry-ийн тэгш хэмтэй таамаглалаар биш.
3. **Rug tail case тусад нь:** dev dump чиний өмнө land хийсэн нөхцөлд curve-ээс гарах бодит үнийг тусад нь тайлагнана. Энэ тоо нь Phase 4 шүүлтүүрийн false negative-ийн жинхэнэ өртгийг өгнө.

**Latency scenario-ууд (бүгдийг зэрэг ажиллуулна):**
- `L0`: 0ms — онолын дээд хязгаар, зөвхөн лавлагаа
- `L1`: 400ms (1 slot)
- `L2`: 1000ms
- `L3`: 3000ms

**Шимтгэл:** pump.fun 1% × 2 тал = 2%. Priority fee + tip-ийг тусад нь параметр болгож 0.001–0.01 SOL хооронд шалгана.

---

## 6. Дискиплин (ажиллуулахаас ӨМНӨ бичигдэнэ)

1. **Frozen holdout:** Хугацааны сүүлийн 30%-ыг тусгаарлаж, Phase 7 хүртэл **огт нээхгүй**. Файлыг өөр directory-д хийж, pipeline-ээс гаргана.
2. **Pre-registration:** Phase бүрийн kill criterion-ыг ажиллуулахаас өмнө git-д commit хийнэ. Дараа өөрчилсөн бол commit history-д харагдана.
3. **Standard error:** Токен ба минутаар **double-cluster**. Энгийн t-test хориотой.
4. **Тайлагнал:** Fat tail тул median, 10% trimmed mean, bootstrap 95% CI гурвуулаа. Дан mean хориотой.
5. **Split-half:** Токенуудыг сондгой/тэгш ID-аар хувааж найдвартай байдлын дээд хязгаарыг гаргана. (Өдрөөр хуваахгүй — токен минутаар амьдардаг.)
6. **Placebo:** Идэвх, curve stage, **цаг хугацаагаар** таарсан санамсаргүй когорт. Гурвуулаа таарууллах ёстой.
7. **Тестийн бүртгэл (v1.1):** Дата руу харсан таамаглал бүрийг `test_log.md`-д бүртгэж commit хийнэ — phase, нүд (босго × τ), огноо, үр дүн. Энэ нь p-hacking аудитыг субъектив таамаглалаас объектив тоолол болгоно: "хэдэн тест хийсэн бэ" гэдэгт git log хариулна.

---

## 7. Phase-ууд, kill criterion-той

### Phase 0 — Дата болон нэгжийн шалгалт
**Гүйцэтгэл:** Claude Code
**Гаралт:** ClickHouse table, шалгалтын тайлан
**Шалгалтууд:**
- Reconstructed `x_post` vs on-chain state — зөрүү < 0.01 SOL байх ёстой (>99% event дээр)
- Curve-ээр бодсон үнэ vs бодит `sol_amount/token_amount` — таарах ёстой
- Universe нь launch-аар тодорхойлогдсон эсэх (migrate хийгээгүй токенууд байгаа эсэх)
- Өдрийн event тоо тогтвортой эсэх (дата дутуу байгаа эсэх)
- **Fee handling (v1.1):** Dune-ий decoded `sol_amount` нь gross (шимтгэл орсон) эсвэл net үү — тодорхойлж баримтжуулна. Хэрэв gross бол curve reconstruction системтэйгээр ~1% зөрөх бөгөөд энэ нь дээрх 0.01 SOL-ын шалгалтыг **жинхэнэ алдаагүйгээр** унагана. x reconstruction тогтоосон тодорхойлолттой нийцэж байгааг тусад нь батална.
- **Дата хэмжээ (v1.1):** Эхний өдөр 90 хоногийн бүрэн universe-ийн event тоог тооцоолж, Dune credit болон локал ClickHouse-ийн багтаамжтай тулгана. Багасгах шаардлагатай бол §2.2-ийн санамсаргүй түүврийн дүрмээр л багасгана.

**KILL:** Curve reconstruction таарахгүй бол цааш явахгүй. Бүх зүйл үүн дээр тогтоно.

---

### Phase 1 — Зардлын загвар (детерминистик)
**Гүйцэтгэл:** Claude Code
**Гаралт:** `cost_model.py` + unit test-үүд
**Агуулга:** §1.1, §5-ийн бүх томъёо (§5.1-ийн exit асимметр багтана)
**Шалгалт:** Түүхэн бодит арилжаануудыг оруулаад, загварын гаргасан үнэ бодит гүйцэтгэсэн үнэтэй таарч байна уу. Зөрүү > 0.5% бол загварт алдаа бий.

**KILL:** Байхгүй. Энэ бол арифметик, ажиллах ёстой.

---

### Phase 2 — Overhead supply (детерминистик)
**Гүйцэтгэл:** Claude Code
**Гаралт:** Токен бүрийн event бүр дээрх `OH(t)`, `OH_ratio(t)`, `OH_conc(t)`
**Хэрэгжүүлэлт:** Токен бүрээр event-үүдийг дараалуулж, wallet → (tokens, cost_basis) dict-ийг incremental шинэчилнэ.
**Шалгалт:** Σ tokens_held ≤ нийт нийлүүлэлт. OH ≥ 0 үргэлж. 0 ≤ OH_conc ≤ 1.

**KILL:** Байхгүй.

---

### Phase 3 — ТООЛОЛТ (загваргүй) ⭐ ХАМГИЙН ЧУХАЛ
**Гүйцэтгэл:** Claude Code
**Хугацаа:** 5–7 хоног (v1.1: power analysis нэмэгдсэн)
**Энэ phase судалгааны амьдралыг шийднэ.**

**3a. Baseline + power (v1.1):**
- `fwd_net_flow(τ)`-ийн нөхцөлгүй тархалт (median, quantile-ууд)
- **Power analysis — 3b-ээс ӨМНӨ.** Baseline тархалтаас bootstrap-аар minimum detectable effect бод: одоогийн sample size, double-cluster SE-тэй нөхцөлд, 3-р зүйлийн эдийн засгийн босго хэмжээний d10−d1 зөрүүг илрүүлэх power ≥ 80% мөн үү?
- **Хэрэв power < 80%:** kill criterion-ий "CI тэгийг агуулна" гэдэг нь "сигнал байхгүй" биш "**дата хүрэлцэхгүй**" гэсэн үг. Тэр тохиолдолд universe-ээ томсгоно (илүү урт хугацаа, эсвэл түүврийн хувийг өсгөх) — судалгааг үхүүлэхгүй. Энэ ялгааг тайланд тодорхой заана.

**3b. Decile тест:** Бүх burst event-ийг `OH_ratio`-оор 10 бүлэг болгож, бүлэг бүрийн `fwd_net_flow(5s)`-ийн median болон bootstrap CI гарга.

**3c. Hazard:** `P(burst dies at age a | alive at a)` — a = 1..30 секунд. Тогтмол уу, өсдөг үү?

**3c-bis. Stratified hazard (v1.1, ЗААВАЛ):** Мөн hazard-ыг `OH_ratio`-ийн tercile-ээр гурав хувааж зэрэгцүүлнэ. Судалгааны гол таамаглал нь яг "ачаа их үед burst хурдан үхдэг" тул үүнийг шууд харах ёстой. Хэрэв өндөр-OH tercile-ийн hazard curve нь бага-OH-ээс системтэйгээр дээгүүр байвал энэ нь 3b-ийн decile тестээс **хамааралгүй, хоёр дахь бие даасан нотолгоо**. Загвар биш, тоолол хэвээр.

**3d. Sensitivity:** Burst босго 0.05x / 0.10x / 0.20x; τ = 2s/5s/15s. §4.1-ийн үндсэн нүд шийдвэр гаргана; эдгээр нь robustness. `OH_conc`-ийн double sort мөн энд (robustness, шийдвэрт оролцохгүй).

**3e. Rug-conditioned split (ЗААВАЛ):** Түүхэн дата дээр rug эсэх нь ex-post мэдэгдэнэ — Phase 4-ийн классификатор хэрэггүй, бодит үр дүнг label болгоно. 3b, 3c-г **гурван багц дээр давтана**:
- бүх токен
- rug-уудыг хассан "цэвэр" багц
- зөвхөн rug-ууд

Энэ нь хоёр зүйлийг зэрэг өгнө: (1) сигнал бохирдолгүй орчинд байгаа эсэх, (2) Phase 4-ийн шүүлтүүр хэр чухал болох. Хэрэв сигнал зөвхөн цэвэр багц дээр л гарвал → шүүлтүүр зайлшгүй. Хэрэв гурвуулаа ижил бол → шүүлтүүр зөвхөн алдагдал хамгаалагч, сигналын нөхцөл биш.

> **Дарааллын тайлбар:** Ажиллах үеийн дараалал (шүүлтүүр → урсгал → ачаа → зардал) болон судалгааны дараалал өөр. Судалгааг эрсдэлээр эрэмбэлдэг: хамгийн их магадлалтай үхлийн цэгийг хамгийн түрүүнд шалгана. Шүүлтүүрийг эхэлж барих нь, сигнал байхгүй байж болзошгүй зүйлд долоо хоног зарцуулах эрсдэлтэй. 3e нь энэ хоёр дарааллыг эвлэрүүлж байгаа хэсэг.

**KILL CRITERION (урьдчилж бичигдсэн, v1.1-д хоёр нөхцөлтэй):**

> **(i) Статистик:** Үндсэн нүд дээр (0.10x, τ=5s) decile 10 ба decile 1-ийн `fwd_net_flow(5s)` median-ийн зөрүүний bootstrap 95% CI нь тэгийг агуулж байвал → `OH_ratio` нь signal биш. Түүнийг хасаад f1–f5 дээр давтана. Тэдгээр нь ч мөн адил бол → **бүх санаа зогсоно.**
>
> **(ii) Эдийн засаг (v1.1):** CI тэгийг агуулаагүй ч, decile 10-ийн median `fwd_net_flow(5s)`-ээс гарах үнийн хөдөлгөөн `(1 + ΔV/x)² − 1` нь лавлагаа нөхцөл дэх BE-ээс (x=50, q=1 SOL, L2 latency, fees=0.02) **бага** бол → сигнал статистикийн хувьд бодит ч ашиглагдахааргүй. Phase 4–6-д гурван долоо хоног зарцуулаад Phase 7 дээр үхэхийн оронд энд зогсоно.
>
> Хоёулаа давсан тохиолдолд л цааш явна.
>
> **Үл хамаарах зүйл:** 3a-ийн power < 80% бол (i)-ийн FAIL нь kill биш — дата нэмэх шийдвэр.

**Hazard-ийн тайлбар:**
- Тогтмол hazard → "хоцрох" гэсэн ойлголт байхгүй → аль ч мөчид орох нь адил → **хурдны уралдаан биш**
- Өсөх hazard → хоцрох нь бодит → орох цонхны өргөнийг тоогоор гаргана
- Энэ тоо Phase 8 ба §12.4-ийн гарах хугацааны хязгаарыг шууд өгнө

---

### Phase 4 — Rug filter (давтагдсан ажил)
**Гүйцэтгэл:** Claude Code
**Лавлагаа:** MemeTrans (arXiv 2602.13480)
**Label:** Rug/high-risk эсэх (dev dump, LP татагдсан, эсвэл launch-аас хойш N цагт бүрэн үхсэн)

**Feature бүлгүүд:**
1. *Эзэмшлийн төвлөрөл* — launch-ийн дараах эхний N минут дахь Gini / HHI / дээд-10 хаягийн эзлэх хувь
2. *Санхүүжилтийн graph* — олон хаяг нэг эх сурвалжаас SOL авсан эсэх; connected component-оор "жинхэнэ оролцогчийн тоо"
3. *Wash trading шинж* — хаягийн buy/sell эргэлтийн харьцаа, арилжааны хэмжээний давтагдал, хугацааны зайн жигд байдал
4. *Dev-ийн зан төлөв* — үүсгэгчийн эзэмшил, түүний өмнөх токенуудын түүх

**Загвар:** Logistic regression → gradient boosting. Neural network битгий, тайлбарлагдах чанар нь илүү үнэтэй.
**Хэмжүүр:** Precision @ өндөр recall. False negative (rug өнгөрөх) нь false positive-ээс хамаагүй үнэтэй. Өртгийг §5.1.3-ийн tail тоогоор илэрхийлнэ.

**KILL:** AUC < 0.65 бол feature-үүдээ дахин үзнэ. MemeTrans-ийн үр дүнг давтаж чадахгүй бол хэрэгжүүлэлтэд алдаа бий.

---

### Phase 5 — Нөхцөлт загвар
**Гүйцэтгэл:** Claude Code
**Урьдчилсан нөхцөл:** Phase 3 эерэг (хоёр нөхцөл хоёулаа)
**Загвар:** `P(fwd_net_flow(τ) > threshold | f1..f11)` — logistic regression эхлээд, дараа нь gradient boosting
**Validation:** Time-series walk-forward. Random split **хатуу хориотой**.

**Embargo (v1.1, ЗААВАЛ):** Feature нь 10s хойшоо, label нь 15s урагшаа хардаг тул split хилийн ойролцоох event-үүд хоёр талд давхцсан мэдээлэл агуулна. Train/test хооронд дор хаяж `max(feature цонх) + max(τ)` = **25s embargo** тавина. Мөн "нэг токений бүх event нэг талд" гэсэн дүрмийг split кодын дотор **assertion** болгоно (Codex §8.4-ийн 4-р шалгалттай давхардана — санаатай).

**Тайлагнал:** Decile бүрийн бодит fwd_net_flow, calibration plot, feature importance

**KILL:** Out-of-sample дээр decile 10 vs decile 1 ялгаа алга бол → загвар in-sample noise барьсан.

---

### Phase 6 — Wallet cluster (бүдүүн туг)
**Гүйцэтгэл:** Claude Code
**Урьдчилсан нөхцөл:** Phase 5 эерэг
**Хэрэгжүүлэлт:**
1. Санхүүжилтийн graph бариx: хаяг A хаяг B-д анх SOL илгээсэн → ирмэг
2. Connected component = cluster
3. `cluster_seen` (f12) тугийг тооцох
**Тест:** Phase 5-ийн загварт f12 нэмэхэд out-of-sample AUC/decile spread **сайжирч байна уу**

**Механик confound-ийн засвар:** Cluster-ийн "чанар"-ыг үнэлэхдээ тухайн арилжааны өөрийнх нь curve impact-ыг хассан үнэ дээр markout бод. Мөн арилжааны хэмжээгээр нөхцөлдүүл — эс бөгөөс "мэдээлэлтэй" биш "том" гэдгийг л хэмжинэ.

**KILL:** Сайжруулалт байхгүй бол f12-ыг хая. **Энэ нь бүх санааг үхүүлэхгүй** — бүдүүн туг сул хэмжигдэхүүн тул сигнал байсан ч барихгүй өнгөрч болно. "Дараа буцаж ирнэ" гэж тэмдэглээд цааш.

---

### Phase 7 — Эдийн засгийн тест (holdout нээгдэнэ) 🔓
**Гүйцэтгэл:** Claude Code
**Энэ бол holdout-ыг нээх ганц цэг.**

Phase 5/6-ийн загварын дээд decile дээр §5-ийн бүрэн зардлын загвараар (§5.1-ийн exit асимметр багтаасан) `fwd_net_ret` бод — L1/L2/L3 latency scenario тус бүрд.

**Тайлагнал:**
- Latency scenario бүрийн expectancy (median, trimmed mean, bootstrap CI)
- Ашгийн төвлөрөл: дээд 1%, 5% арилжаа нийт ашгийн хэдэн хувийг үүсгэсэн
- Ямар curve depth (x) дээр expectancy эерэг байна → §12.3-ийн босгыг өгнө
- Позицийн хэмжээ q-ийн мэдрэмж: q = 0.5, 1, 2, 5 SOL → §12.3-ийн slippage хязгаарыг өгнө
- Exit fill-ийн бодит зардал vs тэгш хэмтэй таамаглал (зөрүү хэдэн %)

**KILL CRITERION:**
> Аль ч бодитой latency scenario (L1–L3) дээр дээд decile-ийн expectancy эерэг биш бол → **систем эдийн засгийн хувьд ажиллахгүй.** Загвар статистикийн хувьд ажиллаж байсан ч энд зогсоно.

**Хэрэв зөвхөн L0/L1 дээр эерэг бол** → хурдны уралдаан. Чиний ялах магадлал бага. Энэ нь бас хүчинтэй, үнэ цэнтэй хариулт. §12-ийн систем энэ тохиолдолд **баригдахгүй.**

---

### Phase 8 — Paper trading
**Урьдчилсан нөхцөл:** Phase 7 эерэг
**Хэрэгжүүлэлт:** §12-ийн бүрэн систем real-time сигнал үүсгэж бичнэ, гүйцэтгэхгүй. Дараа нь бодит үнэтэй тулгана.
**Хугацаа:** Дор хаяж 4 долоо хоног, 100+ сигнал
**Хэмжих зүйл:** Backtest-ийн таамагласан entry үнэ vs бодит үнэ. Зөрүү > 30% бол fill model буруу.

Зөвхөн энэ давсны дараа бодит капитал, бас маш жижиг хэмжээгээр.

---

## 8. Багажийн үүрэг хуваарилалт

### 8.1 Claude (chat) — Судалгааны удирдагч
- Phase бүрийн спецификаци, kill criterion боловсруулах
- Үр дүнг тайлбарлах, дараагийн алхмыг шийдэх
- ChatGPT-ийн аудитын дүгнэлтийг үнэлэх (бүх шүүмж зөв байх албагүй)
- Kill эсэх шийдвэрийг гаргах

### 8.2 Claude Code — Гүйцэтгэл

**Prompt template:**

```
Solana bonding-curve flow research, Phase <N>-ийг хэрэгжүүл.

Хавсаргасан spec v1.1-ийн §<холбогдох хэсгүүд>-ийг уншаад:

1. Дараах файлуудыг бич: <жагсаалт>
2. Бүх feature backward-looking байх ёстой. t мөчид block_time > t
   өгөгдөл ашиглах нь bug — тэр шалгалтыг unit test болгож бич.
3. Standard error-ийг токен болон минутаар double-cluster хийж бод.
4. Үр дүнг median + 10% trimmed mean + bootstrap 95% CI гурвуулаар
   тайлагна. Дан mean битгий гарга.
5. Kill criterion-ий үр дүнг тодорхой гарга: PASS эсвэл FAIL.
   Phase 3 бол статистик (i) ба эдийн засаг (ii) хоёрыг тусад нь.
6. Ажиллуулахын өмнө kill criterion-ыг commit хий.
7. Дата руу харсан тест бүрийг test_log.md-д бүртгэ (§6.7).

Хэрэв spec тодорхойгүй хэсэгтэй бол таамаглаж бүү бич — асуу.
```

### 8.3 ChatGPT — Судалгааны аудит

**Оруулах:** spec + тухайн Phase-ийн үр дүн + код.
**Оруулахгүй:** судалгааны удирдагчийн тайлбар, хүлээлт, чатын түүх. Аудитор хүлээлтийг мэдвэл түүнийг дэмжих хандлагатай болно.

**Prompt template:**

```
Чи бол шүүмжлэгч аудитор. Доорх судалгааны дизайн болон үр дүнг уншаад,
энэ судалгаа өөрийгөө хуурч байгаа бүх зам мөрийг ол.

[spec + Phase-ийн үр дүн + код]

Тусгайлан шалга:

1. LOOKAHEAD BIAS. Аль feature ирээдүйн мэдээлэл агуулж байна вэ?
   Цонхны хил (inclusive/exclusive) зөв үү? Label болон feature-ийн
   хугацаа давхцаж байна уу? Walk-forward embargo хангалттай юу?

2. SURVIVORSHIP. Universe launch дээр тодорхойлогдсон уу, migration
   дээр үү? Үхсэн токенууд дата дотор байна уу? Хэрэв түүвэрлэсэн
   бол түүвэр идэвхээс хамаарсан уу?

3. P-HACKING. Хэдэн таамаглал шалгасан бэ (test_log.md-тэй тулга)?
   Үндсэн нүд урьдчилж зарлагдсан уу? Босго, цонхны сонголт үр дүнг
   харсны дараа өөрчлөгдсөн үү? (git history шалга)

4. SE ба ТАРХАЛТ. Кластерлалт зөв үү? Fat tail дээр mean ашигласан
   уу? Bootstrap зөв хийгдсэн үү? Power хангалттай юу — null үр дүн
   "сигнал байхгүй" мөн үү, эсвэл "дата хүрэлцэхгүй" юү?

5. FILL ASSUMPTION. Backtest-ийн entry үнэ бодитой юу? Өөрийн impact
   тооцогдсон уу? Exit дээр урсгалын нөхцөлт сөрөг чанар тооцогдсон
   уу? Latency зөв загварчлагдсан уу?

6. МЕХАНИК CONFOUND. "Мэдээлэлтэй" болон "том/дагагдсан" хоёрыг
   ялгаж чадаж байна уу? Илэрсэн хамаарал нь шалтгаан уу, эсвэл
   AMM-ийн механикаас автоматаар гарч байна уу?

7. ХАМГИЙН ЧУХАЛ: Энэ үр дүн ХУДАЛ байх хамгийн үнэмшилтэй
   тайлбар юу вэ? Тэр тайлбарыг хэрхэн шалгах вэ?

Сайшаал битгий бич. Зөвхөн сул талыг ол. Юу ч олдохгүй бол тэгж хэл.
```

### 8.4 Codex — Код хяналт

`docs/code_review_prompt.md` болгож repo-д хадгална.

```
Доорх кодыг шалга. Энэ бол санхүүгийн судалгааны код тул нэг
off-by-one алдаа бүх дүгнэлтийг худал болгоно.

[код]

Тусгайлан шалга:

1. TEMPORAL LEAKAGE. Trailing цонх үнэхээр trailing уу? Аль ч
   газарт t мөчийн feature-т block_time ≥ t өгөгдөл орж байна уу?
   pandas rolling/shift-ийн хил зөв үү? Embargo хэрэгжсэн үү?

2. CURVE MATH. x·y=k хэрэгжүүлэлт зөв үү? Virtual reserve
   (x₀=30, y₀=1.073e9) зөв ашиглагдсан уу? Buy/sell тэгш хэмтэй
   хэрэгжсэн үү? Мөнгөн дүнгийн floating point алдаа хуримтлагдаж
   байна уу (Decimal хэрэгтэй юу)?

3. COST BASIS. Wallet-ийн cost basis шинэчлэлт зөв үү? Sell гарахад
   cost basis буруу өөрчлөгдөж байна уу? Дахин худалдан авалт зөв
   дундажлагдаж байна уу?

4. TRAIN/TEST SPLIT. Split цаг хугацааны дарааллаар хийгдсэн үү?
   Нэг токений event-ууд хоёр тал руу хуваагдсан уу (энэ leakage)?
   Assertion байгаа юу? Holdout үнэхээр тусгаарлагдсан уу?

5. SLOT ORDERING. Нэг slot доторх event-ууд tx_index-ээр зөв
   дараалсан уу? Slot хил дээр цонхны тооцоо зөв үү?

6. AGGREGATION. groupby дараа index зөв үлдсэн үү? Merge-ийн
   дараа мөр үржиж байна уу?

Алдаа олбол яг мөрийн дугаартай нь заа. Засвар санал болго.
```

---

## 9. Хугацааны төлөвлөгөө

| Phase | Ажил | Хугацаа | Хуримтлагдсан |
|---|---|---|---|
| 0 | Дата + шалгалт | 4–7 хоног | 1 долоо хоног |
| 1 | Зардлын загвар | 2 хоног | 1.5 |
| 2 | Overhead supply | 3 хоног | 2 |
| **3** | **Тоолол ⭐** | **5–7 хоног** | **3–3.5 долоо хоног** |
| 4 | Rug filter | 5 хоног | 4.5 |
| 5 | Нөхцөлт загвар | 7 хоног | 6 |
| 6 | Cluster туг | 5 хоног | 7 |
| 7 | Эдийн засгийн тест | 3 хоног | 7.5 долоо хоног |
| 8 | Paper trading | 4 долоо хоног | 11.5 долоо хоног |

**Phase 3 дуусахад (~3.5 долоо хоног) чи энэ судалгаа амьд эсэхийг мэднэ.** Ихэнх эрсдэл тэнд шийдэгдэнэ.

---

## 10. Зардал

| Phase | Хэрэгтэй зүйл | Зардал |
|---|---|---|
| 0–7 | Dune, локал машин, ClickHouse | ~$0–50 |
| 8 | Тасралтгүй ажиллах VPS | ~$20–40/сар |
| Дараа | gRPC, colocation | Зөвхөн Phase 7 эерэг бол |

**Дүрэм:** Мөнгө зарцуулах бүх зүйл судалгааны эерэг үр дүнгийн ДАРАА ирнэ. Сөрөг үр дүн гарвал алдсан зүйл бараг байхгүй.

---

## 11. Ямар үр дүн ч ялалт

Энэ судалгаа гурван боломжит хариулттай, гурвуулаа үнэ цэнтэй:

**А. Эерэг, өргөн цонхтой** → тоглож болох тоглоом. Phase 8 руу.

**Б. Эерэг, гэхдээ зөвхөн 1–2 slot** → хурдны уралдаан. Оролцохгүй байх нь зөв шийдвэр. Судалгаа өөрөө үнэ цэнтэй хэвээр.

**В. Сөрөг** → AMM дээр богино хугацааны урсгал үргэлжлэхгүй гэдгийг зөв аргачлалаар няцаасан. Энэ бол нийтлэгдээгүй үр дүн бөгөөд portfolio-д тавихад бүрэн хүчинтэй ажил.

Гурвуулаа мастерийн хөтөлбөрт авч очих боломжтой. Ганцхан муу үр дүн бол дуусгаагүй судалгаа.

---

## 12. Ажиллах үеийн систем (Phase 7 эерэг тохиолдолд)

**Энэ хэсэг нөхцөлт.** Тоонууд нь Phase 5 ба 7-ийн гаралт — одоо зөвхөн бүтэц тодорхойлогдож байна. Phase 7 сөрөг бол энэ хэсэг хэрэгжихгүй.

**Ажиллах дараалал нь судалгааны дараалалтай эсрэг:** судалгаанд хамгийн эргэлзээтэйг эхэлж шалгадаг; ажиллахдаа хамгийн хямд шүүлтүүрийг эхэлж тавина.

### 12.1 Давхарга 0 — Урьдчилсан шүүлт (launch дээр, нэг удаа)
Phase 4-ийн rug классификатор шинэ токен бүр дээр launch-ийн дараа 30–60 секундэд ажиллаж "хяналтын жагсаалтад авах уу" гэдгийг шийднэ. Real-time биш тул хэдэн зуун ms зарцуулж болно. Ихэнх токен энд хасагдана.

### 12.2 Давхарга 1 — Burst илрүүлэлт (тасралтгүй)
Хяналтын жагсаалт дахь токенуудын урсгалыг үргэлжлүүлэн уншиж, §4.1-ийн burst нөхцөл хангагдсан агшинд л дараагийн давхарга руу дамжуулна. Энэ бол trigger, шийдвэр биш.

### 12.3 Давхарга 2–3 — Шийдвэрийн дүрэм

Загварын магадлал өндөр байгаа нь **авах шалтгаан биш**. Шийдвэр нь харьцуулалт:

```
E[ашиг | нөхцөл]  >  BE(x, q, V̂, fees)
```

Баруун тал нь §1.1-ийн breakeven, тухайн агшны curve depth `x`, хэмжээ `q`, хоцролтын үеийн хүлээгдэж буй урсгал `V̂`-аар бодогдоно.

**Гол цэг:** ижил магадлал өөр `x` дээр өөр шийдвэр өгнө. Гүехэн curve дээр slippage их тул өндөр магадлал ч хүрэлцэхгүй; гүн дээр бага магадлал хангалттай байж болно. Тиймээс систем "магадлал > 0.7 бол ав" гэж ажиллахгүй — босго нь агшин бүрд curve state-ээс тооцогдоно. Phase 7-ийн depth-шинжилгээ энэ босгыг өгнө.

**Хэмжээ `q` — хоёр хязгаарлалт зэрэг:**
- *Slippage хязгаар:* `q/x` тодорхой хувиас хэтрэхгүй (Phase 7-ийн q-мэдрэмжээс). Гүехэн curve дээр өөрөө өөрийгөө хөөж авна.
- *Магадлалын жин:* загварын итгэлээс хамаарч хэмжээ өөрчлөгдөнө, гэхдээ бүрэн Kelly биш — fat tail, загварын алдааны тодорхойгүй байдал тул фракциональ (≈¼ Kelly).

### 12.4 Гарах дүрэм — урьдчилж, өөрчлөгдөхгүй
Орохоос **өмнө** тогтоогдож, гараар өөрчлөгдөхгүй:
- Урсгал эргэвэл (`net_flow_1s < 0` тодорхой хугацаанд) → гарна
- Насны хязгаар (Phase 3c/3c-bis-ийн hazard муруйнаас гарсан тоо) → гарна
- Хатуу stop: тодорхой хувийн алдагдал → гарна

Hazard тогтмол бол "хүлээх нь тус болохгүй" → богино хугацаа. Өсдөг бол оновчтой хугацаа тооцогдоно.

### 12.5 Амьд байхад хянах зүйлс
1. **Fill зөрүү:** таамагласан entry үнэ vs бодит (Phase 8-ийн 30%-ийн босго амьд үед ч хүчинтэй)
2. **Calibration drift:** загвар 0.7 гэсэн үед бодитоор 0.7 орчим гарч байна уу
3. **Realized vs expected expectancy:** rolling цонхоор; зөрүү статистикийн хувьд бодит болвол → **автоматаар зогсоно**

Гурав дахь нь зайлшгүй. Ямар ч edge мөнхийн биш — pump.fun-ий дүрэм өөрчлөгдөж болно, бусад оролцогчид ижил зүйлийг олж болно. Автомат зогсолтгүй систем нь edge алга болсны дараа ч арилжаагаа үргэлжлүүлнэ.

---

## 13. Зах зээлийн бүтцийн тухай тэмдэглэл

Энэ зах зээл CFD/futures-ээс бүтцээрээ ялгаатай бөгөөд тэр ялгаа нь давуу тал ба өртөг хоёуланг өгнө.

**Давуу тал:** нэг дүрэм олон мянган бүтээгдэхүүн дээр зэрэг ажиллана → дээж хурдан хуримтлагдана. Futures дээр жилээр хүлээх зүйлийг долоо хоногоор мэднэ. Мөн оролцогчид ихэвчлэн хүн эсвэл энгийн bot — HFT фирмүүд биш.

**Өртөг:**
1. **Олон дээж ≠ олон бие даасан дээж.** Токенууд нэгэн зэрэг халж, нэгэн зэрэг хүйтэрдэг. 5000 ажиглалт нь 200 "халуун цонх"-д багтаж байвал жинхэнэ бие даасан дээжийн тоо 200-д ойр. §6.3-ийн double-cluster яг үүнийг хэмжинэ — техник нарийн ширийн зүйл биш.
2. **Бүтээгдэхүүн бүр богино насалдаг** тул тухайн токенд тохируулга хийх боломжгүй. Дүрэм бүх токенд нийтлэг байх ёстой.
3. **Чадавхи хязгаартай.** q нь curve гүнээр хязгаарлагдана (x=50 үед 2 SOL нь 4% slippage). "Олон дээр жижгээр" гэдэг нь сонголт биш, албадлага.

**Мөн чанар:** futures дээр edge нь нэг үйл явцын *гүнд*, энд олон үйл явцын *өргөнд* байдаг. Тиймээс судалгаа нь cross-section — OH_ratio-г x-д хэвийлэх шаардлага үүнээс гарч байгаа юм.

**Ижил зүйл:** зардлыг давсан edge байх шаардлага. pump.fun-ий 2% round-trip нь futures-ийнхээс өндөр. Олон дээж тэр саадыг давахад тус болохгүй — зөвхөн **хариултаа хурдан мэдэхэд** тус болно.
